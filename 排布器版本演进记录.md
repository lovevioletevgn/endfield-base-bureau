# 排布器版本演进记录

> 本文按**功能域**归档排布器的实现历程：每个模块做了什么、依据是什么、踩过哪些坑。
> 版本号与线上一致；验收数据统一收在文末「回归测试」一节。

---

## 一、功能实现

### 1.1 地基与第一层

- **配方图**：317 条配方按 `machineId` 挂到设施；28 个配方组的权威接口绑定；相态（固 / 液 / 气）
- **产能模型**：`Rrate` / `Rcarriers` / `Rplan`（纯函数）
- **配方树展开**：`Rexplode`（避环 → 排除分离 / 回收 → 子树代价 → 链深上限 → **启动料清单**）
- **摆位** `LawPlan`（按深度分层、物料自下而上、通道高度自适应）
- **连线** `RwRoute`（两阶段：先挑端口预留端点、再统一走线）
- **用电** `Rpower` · **协议容量** `Rbandwidth` · **发电** `Rtheories`
- **① 评价函数** `Rscore` —— 五成本项合成 0~100 分 + 方案并排比较
- **② 第一层收尾** —— 吞吐真并联（含逐段负荷判定）· 防御 / 滑索上限 · 存电

### 1.2 矿点与原料侧（全部闭环，无待核项）

- **矿点数据**（v47）—— 按小地图 × 高低纯度；源矿 60 点 1100/分、紫晶 12、蓝铁 60、赤铜 28（23 高 + 5 低）= 510/分，
  与游戏内 UI 逐项一致（依据武陵简报截图逐区计数）
- **原料侧闭环** —— 原料需求 → 要几台矿机 / 哪个区够 / 水驱矿机耗水 → 水泵台数

### 1.3 查询页

- **瓶装物显示内容物**（v51）—— 灌装 / 拆解配方反推，83 个物品标「装：X（相态）」、空容器标记，可搜索
- **气体散布机环境圈**（v103）—— 对齐游戏功能，散布机生效范围可见、圈色随气体变。数据链：
  `FactoryVaporizerTable.rangeExtend{x:5,y:5,z:5}` → 3×3 本体外扩 5 = **13×13 方形**（实测确认非圆）；
  `FactoryEnvDisplayTable` GenEnv1-4 → 白 / 蓝 / 橙 / 绿（**惰气 / 水蒸气 / 酸气 / 息壤气**，consumeRate 6 · 上限 30）。
  实现：build 注入层把两张表灌进 `DB.blueprint`（同 ⭐content 通道）；渲染层 `.lo-env` 半透明色块 + 同色格线，
  `pointer-events:none` 不挡点击、越界自动裁剪；`LgasSet` 批量换气换色；圈层开关独立于接口开关；
  圈色为近似色（游戏取色不可得，图例注明）
- **v104 环境三连** —— ① **四色按游戏 UI 校准**（稳定青蓝 / 湿润白 / 酸性橙黄 / 息壤翠绿；
  v103 曾按特效资源名猜色、把稳定与湿润对反 —— 资源名是内部命名、非显示色；白圈专属高浓度解决「湿润不明显」）；
  ② **就地选气**（点散布机、机器正上方浮出四色块按钮即换，左栏入口撤除）；
  ③ **环境依赖透明化** —— 探针实锤：排布器从 v1 起就按省料的环境版配方计算（配方选择的子树代价排序天然选中它们），
  但此前从未告知使用者；现报告新增「💨 环境依赖」段 + 配方页 5 条标环境标签 + 帮助补「完全覆盖 / 通气 6 单位每分」官方面板口径；
  `DB.recipeEnv` 经 build 注入层从 `FactoryMachineCraftTable.gasEnv` 补进。**排布计算零改动**
- **v105 修复：点选气条误摆** —— 就地选气条浮在画布内，点色块的 mousedown 会冒泡进画布摆放链
  （清选中 → 松手判「单击空白 + 有 pick」→ Lput 多摆一座）；`LonMouseDown` 入口对 `.lo-gasbar` 目标早退即修。
  顺带把迷你 DOM 的 `closest` 修成沿链逐层匹配（旧一层式 mock 把 `cellEl.closest('.lo-gasbar')` 错判成真值，
  guard 误吞格子 mousedown）
- **v106 修复：传送带 / 管道「一格拐弯」画不出** —— 弯头格三部件自相矛盾：
  进色条按 rot 单值推的「进 = 出的反向」画在上边，弯道弧却按真实拓扑（flowIn 由邻居反推）从左边绕。
  修复 = 进色条与 tooltip 的「进」都改用真实拓扑，与弯道弧同一口径（弯头格 = 「左进下出」L 形，游戏同款观感）；
  管道同一分支一并修；功能件（汇 / 分 / 桥 / 阀）多边进出，不适用单进边，保持按配置表画。
  已知口径：分两段拉线时拐角格要自己按 R 键转（游戏里已铺带子同样手动转）
- **v107 拐弯跟手势 + 孤格带子接机器口** —— ① 拐弯第一轴**跟手势**：记录鼠标轨迹，
  第一个偏移过 1 格的轴先铺（先往上拖 = 先铺竖段）；旧版按总位移大小定轴，想先竖后横会被画成镜像。
  无轨迹 / 斜拖回退旧判定。② 对角机器夹角的孤格带 / 管：出料口外格也算拓扑
  （按介质匹配口），进色条 / tooltip / 弯道弧从机器口反推，不再是默认直箭头。
  实现注记：`LportDir` 的 u/d 与色条体系 t/b 是两套方向命名，`LOGI_OPP['u']` 为 undefined，已转译
- **v108 从机器口（红圈）起手拉带子** —— 按在机器朝外的输出口格上起手，带子从**口外那一格**开始铺、不占口格。
  旧版起手格被机器占着时 `Lfree` 判定失败，压根进不了连铺，「从红圈起手」做不到。
  `LsnapStart` 做成**纯探测**（只返回坐标、不改拖拽态，避免探测污染状态）；整段手势仍只压一次撤销栈。口外被占则不误起手
- **v109 机器口点击分流 + 协议核心出货选择** ——
  ① **点击分流**：手拿物流件按在机器口格上不再直接进 move，而是先只选中进 `portpend` 待决态；
  **原地松手 = 从口外起手连铺**（v108 那条路终于可达）、**拖过 4px 才转 move 移机器**。
  旧版命中机器就往 move 走，想从口红圈拉线一动就把机器拖走 —— v108 的「口外起手」分支永远走不到。
  ② **协议核心出货**：`sp_hub_1` 6 个出料口在**格内**画朝外的可点箭头 `.lo-dlv`，点开列出该核心所属域的可出货物品。
  数据源是 `raw/FactoryItemTable.json` 的 `deliverItemTypeList`（非空 = 可出货，值 `[3,1]`/`[3,2]` 的 1/2 是目标域序号）
  → 注入成 `DB.hubItems` **281 件**（四号谷地 70 / 武陵 211）；缺名的 19 件（掉落物 / 测试件）
  从 `I18nTextTable_CN.json` 按 `name.id` 反查补齐，0 件退 id。选中后面板内的箭头变绿并标出物品名，
  **再点同类 = 取消**；出货记录独立于摆放快照（撤销不影响）。
  ⚠️ `FactoryHubCraftTable.json` 是「造建筑」配方、与出货无关（已排除，勿再当数据源）。
  实现注记：箭头坐标按「格中心 + 内推」两版都越界（9×9 核心在 20px 格下左口格中心仅 10px，left 算出 -1 / -7）
  → 最终**压在口格中心、箭头缩到 12px**；域判定首版错用 `DB.bases.areas`（只有 4 条且**没有 levelId**）
  导致武陵基地永远匹配不上，被新增断言当场抓到 → 改用 `Lbases()`

### 1.4 ③ 单基地「摆得更紧、走得更短」（v50~v63）

- **Dijkstra 转弯罚寻路** —— BFS → 代价寻路（状态含方向、换向 +2），路径又短又直
- **参数搜索 + 按分择优** —— 间{4,3,2} × 对齐{关,开} × 通道{5,3} 12 组全摆打分自动选优，消息报搜索结果
- **桥接器**（参照 IndustrialPlanner Connector 规则）—— 已铺线格允许「正交直穿」，
  穿越格自动铺物流桥 / 管道桥（桥上不转弯、一格桥只跨一条线）
- **就近分组** —— 汇流组按 x 就近配对；失败 warn 带坐标；失败驱动 corr 爬升
- **层内主动换行**（v63）—— 上游比下游宽时（12 台块机 vs 6 台罐机），
  机器按「消费它的下游」分组、**组的 x 起点对齐下游机器**，与行内冲突就下沉一行（自动交错分行）
- **实测**：铁制零件@10 管线 10→6 格（-40%）；赤铜耐压罐@10 **全连通零手动连**；
  赤铜耐压罐@30 **手动连 15 → 0~1**、高度 90→54、线长再降 25%（1149→863 格）；
  生成耗时 0.3~2.6s（参数搜索 4~15 组择优）

### 1.5 试摆画布 · 拉线交互（v56，对齐游戏手感）

- **L 形拉线**：选中传送带 / 管道后拖拽，路径跟手势走（主轴先走、再转第二轴），弯头朝向自动衔接 —— 解决「拐不了弯」；管道同理
- **端点吸附**：起手 / 落点压在机器上时自动吸到「输出口 / 输入口」外一格（游戏里「出口旁边那格开始拉」）
- **弯道圆弧渲染**：弯道格画成 L 形圆弧带（进边 → 圆角 → 出边 + 出口小箭头），直线格保持箭头 —— 渲染时由流向表反推每格进边（v57）
- **分 / 汇流器替换线上段**：拿分 / 汇流器点已有的同类介质物流段 → 删段放分 / 汇（游戏同款替换；跨介质、压机器仍拒绝）（v58）
- **物流桥叠线 / 准入口串线**：桥点带 / 管格 → 叠上（原线保留）；准入口 → 替换那段（v59）

### 1.6 ⑤ 交互与可靠性

- **局部锁定**（v64）—— 锁 =「这台我满意了，别动它」：
  - **交互保护**：锁定件**拖不动 / 删不掉 / 转不了 / 不参与复制**，双击也不会误删；选中后按 `L` 锁定 / 解锁，工具栏有「锁定选中」「解锁全部」
  - **重排约束**：「重排其余」把锁定机器当**固定件**（位置一格不动），其余机器重新分层摆位并**绕开**它们，管线整条重铺；
    台数口径 = 节点台数 − 已锁台数（**机器总数守恒、产量不变**），手摆的散件留原地并被当障碍
- **分流器分支的真实工况覆盖**（v65）—— 扫全库找到真能触发它的三个目标：
  **工业爆炸物 / 实验玉铜发散器 / 息壤玉葫芦**（结构都是「1 台高产能上游要喂 5~10 台下游」）。顺带抓到两个真 bug：
  ① **一台分流器只有 3 个出料格，旧版只摆一个** → 第 4 台下游起**静默丢弃**（既不接线也不报警）；
  现改为**每 3 台下游并排摆一个**（各占上游 1 个出料口）；
  ② 「需要几个分流器」原来按「缺几个出料口」算，量级不对 → 改成 `ceil(下游台数 / 3)`。
  被丢下的线一律**逐条点名**；新增 `route.stats = {split, merge, dropped}`，生成消息报「汇流 X / 分流 Y」。
  实测：息壤玉葫芦@5 **分流器 2 个 → 5 台下游全接上、零手动连**
- **高产能 / 宽链的连通率**（v66）—— 先扫全库 52 个中大型工况量出短板（零手动连 44/52、手动连共 23 条），
  再逐候选拆分数找根因，两个：
  ① **打分口径反了**：2 条手动连只罚 200 分，盖不过 300 格线长差 → 「全连通但线长」的方案被判输。
  手动连权重 **100 → 400**（少一条线是玩家得动手补的功能缺陷，多铺格子只是效率问题）。
  ② **参数网格太窄**：机器间距只到 4，而宽链要「**间 8**」机器之间那条竖缝才够并行走线 →
  加「**失败驱动宽间距扩搜**」（只在还有手动连时才补跑，快路径不花额外开销）+ 汇流器两处口径修正。
  **效果：零手动连 44 → 51/52 例，手动连总数 23 → 1 条**；耗时 3.4~4.1s（只在难例上多花 1~2s）

### 1.7 数据修正（非功能项，但影响排布器读数）

- **矿机的出料口「用不上」，示意图不再画**（v68 / v70 / v71）：
  配置表里矿机挂着 3 个传送带出料口，但**两种模式都用不上**（默认无线回传仓库；切「仓储模式」也只是进缓存区、**手动取出**）
  → 画出来只会让人以为要接带子。现改为在建物上标 **⇡ 无线回传仓库**（电驱 / 二型电驱 / **水驱**）
  或 **⇥ 缓存区 · 手动收取**（便携源石矿机），水驱的**管道进料口**（清水）照常画；`ports` 数据保留、只是不渲染。
  两条更正：① **水驱矿机也是无线回传**（实机确认 —— 配置表里没有该字段，"字段缺失 ≠ 功能不存在"）；
  ② **水驱矿机是 1.1「新潮起，故渊离」（武陵工业二期）新增的，不是 1.5**（联网核实官方公告）。
  `mining_power.json → gather[]` 新增 `wireless` / `wirelessSource` / `msTransferCD` / `wirelessNote` 四个字段
- **免电变体（`_nop_`）连修带剔**（v92 / v93）：
  ① **精炼炉免电版耗电错标修正** —— 原始表 `powerConsume=5` 却缺 `needPower` 标志位，旧版误显「无需通电」，
  规整为 `powerConsume>0` 一律按耗电处理（标志位缺失 ≠ 不耗电，全表仅此一台）；
  ② **考据实锤**：6 台 `_nop_` 变体全部没有建造配方（67 条零命中），是剧情 / 系统内部建筑，建造菜单造不出来；
  ③ **v93 处置**：蓝图页剔除储液罐免电版（与普通版卡面一字不差、纯撞卡），其余 5 台保留（供电行可区分）。
  蓝图条目 107 → **106**，建筑设施页 107 不动

### 1.8 参照调研

- **IndustrialPlanner 源码规则提炼** —— 分 / 汇 / 桥生成规则、round-robin 轮转语义、五阶段仿真节奏，
  详见《IP 源码规则提炼.md》；桥接器与 round-robin 标注已照此落地

---

## 二、收官项与明确不做

### 2.1 ③ 可选尾巴（已收 —— v91）

- **按层级换行（跨料交错的地基）**：down 模式组内机器**到右墙折行**（回组起点、行下移继续摆）、冲突失败换行重试 ——
  旧版撞墙 `break` **静默丢机器**：赤铜耐压罐@30 只摆 19/30 台且报告零警告，赤铜块@10 丢 2 台后同样如此。
  修复后罐@30 **30/30 全摆、手动连 0**；组间同行穿插（跨料交错）由格级冲突检测天然支持，电池链已验到跨料同行

### 2.2 ⑤ 交互与可靠性（已全部收官 —— v64~v66）

见 §1.6。

### 2.3 ④ 原料侧闭环（已收 —— 能做的做完了，两项明确不做）

复核结论：**不按原方案做**。

**已落地**：水驱矿机的**供水 / 供电配比清单**（口径：**1 台水泵最多带 3 台水驱矿机满效率**）——
泵台数 = `⌈矿机数 ÷ 3⌉`、前 N−1 台各带 3 台 / 最后一台带余数、**每台泵 1 条管道就够**（60 < 管道上限 120）、
水泵要通电 10 电 / 台（水驱矿机靠清水自供能、不耗电）。报告里直接给。
（原料需求 → 要几台矿机 / 哪个区够 / 占用比例 早已在报。）

**明确不做**（原因不是"卡数据"，是**问题本身不成立**）：

- ~~矿点逐点坐标~~ → 三种矿机产物都是**无线回传仓库 / 缓存区手动取**，**野外没有需要连的线**，
  每个矿点独立放一台就完事 —— 没有连线 / 避让 / 优化，也就没有"排布问题"，坐标对排布器没有增益
  （只对"打开地图照着放"的清单有用，那不是这个工具干的事）。
- ~~供水管网走线~~ → **野外不是网格**，配置表里也没有地形与障碍（只有单段线长上限：供电桩 30m / 中继器 80m），
  水管怎么绕、电从哪拉只能看地形；能算的只有配比与数量，已给出。

### 2.4 ⑥ 多基地 / 全区域（三刀全落）

**⑥-1 跨地区收货**（v73 起，工具栏开关，默认关）：

开启后，链里的原料只要「能被跨地区传输」（物品表 `transferDomainIds` 那 243 件），就按
**四号谷地超库存传输过来**处理，本地**不建它的产线、不算它的矿机配比**。

- **机器台数与配方不变**（收货只改"料从哪来"）；收货的料仍留在原料清单里
- 报告给「需多少/分 · 单位物品价值 · 每批间隔」，并把界面上的**传输总值**填进来才反推
  「每批能带多少 / 供货速率多少」—— **每档具体多少配置表里没有**（`DomainDataTable` 的建设等级效果
  只有 bandwidth / battleBuildingLimit / travelPoleLimit / isMineOutputUp），所以不填就不给数字
  （v75 联网回填实锤：**11 级 1200 / 12 级满级 1500**，NGA×2 + GameRant 多源一致）
- **配套修掉一处自相矛盾**：原来「原料野外上限」和「原料侧闭环」还在按本地矿脉算
  「全图上限 / 要 1 台矿机」—— 同一份报告里既写着"跨地区收货"又写着"去野外摆矿机"。
  现在统一标 **本地不采、不计野外上限、不配矿机**
- **v74 真机回归修掉一处单位错**：供货速率少除 60（「个/小时」标成了「/分」），「喂不饱」红字永不触发。
  教训：期望值必须独立推单位，不能照实现抄
- **v75 联网核实补充**：解锁等级（**8 级**库存传输 / **12 级**超库存，GameWith+game735 双源；
  smzdm 孤源写 11）、档位（**1200 / 1500**）、**升 12 级后协议不自动切换**要手动改（NGA+smzdm 双源）、
  取消重设重置 1 小时计时（GameRant 单源）、传输在线才推进（NGA 单源，待实机确认）—— 详见 README §六
- **v79 单选修正**：「布局试摆里跨地区传输没给选择传什么物品的选项」→ 查实是 v73 简化错了。
  一条传输路线**一次只能传一种物品**（协议管理里 Edit → 选一种 → 启动；换物品 = 停止重设、计时重置回 1 小时；
  GameRant / GameWith / Game8 三源一致）——「能传的自动全收」**不成立**。
  改成：链里可传原料 ≥2 种时**只挑一种**走传输（默认**需求最大的**，最值得省的产能），其余回退**本地自产**；
  报告里出**选择器**随时换选（单候选不显示）。
  真机：中容谷地电池@1（蓝铁矿 10/分 + 源矿 15/分 双可传）→ 默认收源矿，换选蓝铁矿后源矿回退「矿机 20/分」
- **v80 选货网格**：原生 `<select>` 换成**候选卡片网格** —— 每卡：稀有度色条（`rarity` 1灰/2绿/3蓝/4紫/5金/6红）
  + 物品名 + 需求/分 · 单位价值 · 每批能带几个，选中打勾，**点卡即重算**；单候选也渲染选中卡，标题只在 ≥2 候选时出现。
  本库离线无图标资源 —— 色条与勾都是真实数据 / 状态，不造假图标、不引外部图片。
  **顺藤修掉 v73 起的「假开关」**：点收货开关原来只改文案不重算（报告挂旧 plan）→ 现改为开关即重算，
  与 LshipPick 同款，关掉同时清选状态
- **v81 链上全物品可选 + 面板即选**：
  ① 候选不再限定原料叶 —— 链上任何能传的物品都能选（半成品卡上标「半成品」；选中半成品 = 它的整棵上游子树
  不用建，引擎本来就支持：收货物被剔除出配方表 → 落原料并标收货）。实测电池链 7 件全可传，换选铁制零件后
  蓝铁块 / 蓝铁矿的机器全消失；目标本身不在候选里。② 选货条提到**产线面板开关正下方**，开关一开立刻能选
  （没生成产线也显示 —— 轻量展开算候选，不摆机器不动画布）；有产线时面板让位给报告（不出现两个网格）。
  换选补 plan 守卫：没生成产线时点卡只换选，不悄悄生成产线覆盖手摆画布
- **v82 全量可传清单 + 折叠搜索**：
  联网三源核实（Game8 / GameWith / Game8）：游戏里协议管理的候选 = **出发地能产出的全部物品**（解锁过产能就行，
  仓库有没有无所谓；武陵特产传不了）—— v81 的「链上可传」只是子集。候选扩成 链上可传 ∪ 全库「有机器配方
  且能送到」162 件 + 链上原料叶单独补（矿机产出也算地区产能）；排序：链缺原料 → 链上半成品（需求降序）→
  其余全量（名称序）收进**折叠区**带搜索框（160+ 件全平铺会撑爆面板）；**换目标立刻重算候选**；
  链外选中报告明说「只进仓库、产线不变」（明细 0 行不至于像没生效）。
  真机：平铺 5 + 折叠 158 → 搜索 158→25 → 链外选中生成（机器与基线一致 + 提示）→ 换目标候选即刷

**⑥-2 多目标共享中间产物**（v76，工具栏「＋ 目标」，最多加 3 个附加目标）：

主目标之外再填目标与速率，**所有目标放进同一张需求 DAG** —— 共用中间料只建一套产线，
出料再分流给各目标，不用为每个目标把同一条上游链各摆一遍。

- 实现：`Rexplode` 增加 `opt.seeds`（多种子）+ 按物品 memo 去重；中间料第一次被需要时正常建产线，
  之后每个再要它的地方挂一条**幽灵边**（引用同一节点、记着「谁在要」）；首个出现处也包成幽灵边，
  所有消费边同构 —— 分流器 / 合并器、按消费者的管线统计直接复用 ⑤-2 机制，`RwRoute` **零改动**
- 合并需求统一重算：`RexplodeFinal` 按 Kahn 拓扑把「父节点整台实出 × 配比」灌回共享节点，
  台数 = ⌈合并需求 / 单机速率⌉；共享节点深度取**最深消费者 + 1**；两轮推演（原始 / 带进货）口径不变
- 报告：头部目标从 1 个变 N 个（「 ＋ 」连接）；新增**共用中间料**段（几台 · 合并需求 · 整台实出 → 分流给谁多少/分）；
  原料清单取各目标并集；共用节点在管线明细里按消费者各给一行
- **口径提醒：合并不保证省机器** —— 整台进位不重合时合并台数 = 分开建之和，
  省的是**同一套上游产线只摆一遍**：一条矿线、一套水线，出料再分流
- 实测：赤铜耐压罐@10 ＋ 赤铜瓶@10 → 共用赤铜块 8 台（合并需求 40/分）、共用清水 8 台；
  ⚠️ 这种双目标长链默认 50×50 画布放不下（守卫提示「试了 N 组参数都越界」是**按设计触发**），换 80×80 即可

**跨基地选点**（v83，**分配建议器** —— v1 只出建议不摆画布）：

报告尾部常驻「哪个成品放哪片基地更省」，布局面板「选点建议」开关同款。收货方向改「从（）到（）」双下拉
（清单动态读 domains，未来新地区零改动接入）；两地对称互传 —— 单端下拉选另一端 = 换向；
切基地时「到」自动对齐所在地区。成本 = 2^N 组合穷举：硬否决 > 同方向多种收货物 > 口径①喂不饱 >
分两地重复建共享料 > 矿缺口量。两口径都入模：
① 地区合计需求对每批可到货反推（罐@30 全放谷地 → 赤铜矿缺 60/分 vs 供货 25/分 → **喂不饱**）；
② 取货口 3×1×3、路数 =（边长−1）÷3（谷地 23/13 实测、武陵 26/16 推算），吞吐无单列数据 → 明说不编。
「不能跨地区传输 + 产线未建模」的料（惰性壤晶废液 80/分、惰气 10/分）**点名给量不静默**。
现有单链公式（台数 / 收货反推 / 摆位）**不受粒度影响**，一行没改。

- ⚠️ **硬约束**：集成工业系统**同时只在至多两个地区运行**；
  仓库分地区且互相独立，**一地区一个仓库、区内几片基地共享** —— 收发粒度是「地区」不是「基地」；
  跨地区传输**每批只能传一种物品**，数量上限 = 传输总值 ÷ 单位物品价值；
  批次间隔 `FactoryConst.domainTransportIntervalTime = 3600` → ⑥ 的「多基地」实际是**两地联动**。
  **工具口径**：只按**超库存传输**建模、按**满级**取传输总值 **1500/批**，11 级 1200 档不进工具

---

## 三、当前状态

**③ 单基地优化全部收官**（赤铜耐压罐@30 手动连清零、线长 -25%），
**⑤ 交互与可靠性也全部收官**（局部锁定 v64 · 分流器分支 v65 · 高产能连通率 v66 —— 全库 52 个中大型工况里 **51 个零手动连**），
**④ 也已收口**（配比清单给了，两项明确不做），
**⑥ 三刀全部收官**（跨地区收货 v73-v82 —— 原料能从对面地区超库存传输过来，一条路线一次只传一种；
多目标共享中间产物 v76 —— 共用中间料只建一套再分流；跨基地选点建议器 v83 —— 方向双下拉 + 对称互传 +
2^N 组合穷举给「哪个成品放哪片基地更省」，两口径全入模）。
**③ 最后一条可选尾巴也已收**（v91 按层级换行）。

⚠️ v91 顺带纠偏：⑤-3 的「51/52 零手动连」曾含假绿成分
（部分工况靠 down 候选静默丢机器清零）—— 修复后按「全摆优先」复验：罐@30 真清零（30/30 · 手动连 0），
赤铜块@10 是 12/12 全摆 + 手动连 2（诚实提示，不再用丢机器换假 0）。**全部条目收官，无剩余尾巴。**

收官后新增对齐游戏的功能：**v103 气体散布机环境圈**（13×13 方形 · 四气四色 · 可换气）。

> ⚠️ **一处真机才会触发的 bug 修复**：`RwRoute` 里 `const outNeed` 写在了 `if` 块内、却在 `else if` 里引用 ——
> 老测试把 `const` 换成 `var` 跑，块级作用域错误被**掩盖**，浏览器里一跑 `LawRun` 就 `outNeed is not defined`（排布器整个点不动）。
> 现在 `test_html.js` 多了一道「**用原始代码（const/let 版）冒烟跑关键路径**」的门禁守着这类问题。

---

## 四、回归测试

所有功能改动都带回归断言。以下为各阶段的验收规模（`test_html.js` 日常档，另含 `test_layout_events.js` 事件层）：

| 版本 | test_html | 事件层 | 增量说明 |
|---|---|---|---|
| v64 局部锁定 | 553 | 72 | +33 锁定保护 / fixed 避让 / 重排守恒 |
| v65 分流器分支 | 565 | 72 | +11 含大产线「摆不下如实报数」 |
| v66 连通率 | 572 | 72 | 打分权重与参数网格修正 |
| v73 跨地区收货 | 600 | 72 | +15 数据层 / 判定 / 开关对照 / 间隔常量 |
| v76 多目标共享 | 620 | 72 | +20 共享节点 / 幽灵边 / 需求合并 / ⑥-2×⑥-1 组合 |
| v79 单选修正 | 626 | 72 | +4 单选（另 v77 预填 +2） |
| v80 选货网格 | 628 | 72 | +2 开关即重算 |
| v81 链上全物品 | 631 | 72 | +3 面板即选 |
| v82 全量清单 | 633 | 72 | +2 折叠搜索 |
| v83 跨基地选点 | 667 | 72 | +34 组合穷举 / 两口径 |
| v91 按层级换行 | 668 | — | 两条假绿断言改「全摆优先」真口径 + smoke 同步 |
| v103 环境圈 | 681 | — | +18 注入 / 摆放 / 渲染 / 换气 / 开关 |
| v104 环境三连 | 692 | — | +11 四色校准 / 就地选气 / 依赖透明化 |
| v105 选气条误摆 | 692 | 75 | +2 事件层回归 |
| v106 一格拐弯 | 700 | 75 | +8 拓扑口径 |
| v107 拐弯跟手势 | 706 | 78 | +6 / +3 轨迹与机器口反推 |
| v108 口外起手 | 710 | 81 | +4 / +3 纯探测与口外交接 |
| v109 口上点击分流 + 核心出货 | **732** | **103** | +22 / +13；HEAVY 全量 746 |

> **门禁口径**：日常档 732 项必须全绿、`fail=0` 才允许发布；
> `HEAVY=1` 跑全量（含被跳过的 14 条大链用例）共 746 项。
