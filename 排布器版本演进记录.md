# 排布器版本演进记录

> 本文按**功能域**归档排布器的实现历程：每个模块做了什么、依据是什么、踩过哪些坑。
> 版本号与线上一致；验收数据统一收在文末「回归测试」一节。

---

## 一、功能实现

### 1.1 地基与第一层

- **配方图**：317 条配方按 `machineId` 挂到设施；28 个配方组的权威接口绑定；相态（固 / 液 / 气）
- **产能模型**：`Rrate` / `Rcarriers` / `Rplan`（纯函数）
- **配方树展开**：`Rexplode`（避环 → 排除分离 / 回收 → 子树代价 → 链深上限 → **启动料清单**）
- **摆位** `LawPlan`（按深度分层、物料自下而上、通道高度自适应）
- **连线** `RwRoute`（两阶段：先挑端口预留端点、再统一走线）
- **用电** `Rpower` · **协议容量** `Rbandwidth` · **发电** `Rtheories`
- **① 评价函数** `Rscore` —— 五成本项合成 0~100 分 + 方案并排比较
- **② 第一层收尾** —— 吞吐真并联（含逐段负荷判定）· 防御 / 滑索上限 · 存电

### 1.2 矿点与原料侧（全部闭环，无待核项）

- **矿点数据**（v47）—— 按小地图 × 高低纯度；源矿 60 点 1100/分、紫晶 12、蓝铁 60、赤铜 28（23 高 + 5 低）= 510/分，
  与游戏内 UI 逐项一致（依据武陵简报截图逐区计数）
- **原料侧闭环** —— 原料需求 → 要几台矿机 / 哪个区够 / 水驱矿机耗水 → 水泵台数

### 1.3 查询页

- **瓶装物显示内容物**（v51）—— 灌装 / 拆解配方反推，83 个物品标「装：X（相态）」、空容器标记，可搜索
- **气体散布机环境圈**（v103）—— 对齐游戏功能，散布机生效范围可见、圈色随气体变。数据链：
  `FactoryVaporizerTable.rangeExtend{x:5,y:5,z:5}` → 3×3 本体外扩 5 = **13×13 方形**（实测确认非圆）；
  `FactoryEnvDisplayTable` GenEnv1-4 → 白 / 蓝 / 橙 / 绿（**惰气 / 水蒸气 / 酸气 / 息壤气**，consumeRate 6 · 上限 30）。
  实现：build 注入层把两张表灌进 `DB.blueprint`（同 ⭐content 通道）；渲染层 `.lo-env` 半透明色块 + 同色格线，
  `pointer-events:none` 不挡点击、越界自动裁剪；`LgasSet` 批量换气换色；圈层开关独立于接口开关；
  圈色为近似色（游戏取色不可得，图例注明）
- **v104 环境三连** —— ① **四色按游戏 UI 校准**（稳定青蓝 / 湿润白 / 酸性橙黄 / 息壤翠绿；
  v103 曾按特效资源名猜色、把稳定与湿润对反 —— 资源名是内部命名、非显示色；白圈专属高浓度解决「湿润不明显」）；
  ② **就地选气**（点散布机、机器正上方浮出四色块按钮即换，左栏入口撤除）；
  ③ **环境依赖透明化** —— 探针实锤：排布器从 v1 起就按省料的环境版配方计算（配方选择的子树代价排序天然选中它们），
  但此前从未告知使用者；现报告新增「💨 环境依赖」段 + 配方页 5 条标环境标签 + 帮助补「完全覆盖 / 通气 6 单位每分」官方面板口径；
  `DB.recipeEnv` 经 build 注入层从 `FactoryMachineCraftTable.gasEnv` 补进。**排布计算零改动**
- **v105 修复：点选气条误摆** —— 就地选气条浮在画布内，点色块的 mousedown 会冒泡进画布摆放链
  （清选中 → 松手判「单击空白 + 有 pick」→ Lput 多摆一座）；`LonMouseDown` 入口对 `.lo-gasbar` 目标早退即修。
  顺带把迷你 DOM 的 `closest` 修成沿链逐层匹配（旧一层式 mock 把 `cellEl.closest('.lo-gasbar')` 错判成真值，
  guard 误吞格子 mousedown）
- **v106 修复：传送带 / 管道「一格拐弯」画不出** —— 弯头格三部件自相矛盾：
  进色条按 rot 单值推的「进 = 出的反向」画在上边，弯道弧却按真实拓扑（flowIn 由邻居反推）从左边绕。
  修复 = 进色条与 tooltip 的「进」都改用真实拓扑，与弯道弧同一口径（弯头格 = 「左进下出」L 形，游戏同款观感）；
  管道同一分支一并修；功能件（汇 / 分 / 桥 / 阀）多边进出，不适用单进边，保持按配置表画。
  已知口径：分两段拉线时拐角格要自己按 R 键转（游戏里已铺带子同样手动转）
- **v107 拐弯跟手势 + 孤格带子接机器口** —— ① 拐弯第一轴**跟手势**：记录鼠标轨迹，
  第一个偏移过 1 格的轴先铺（先往上拖 = 先铺竖段）；旧版按总位移大小定轴，想先竖后横会被画成镜像。
  无轨迹 / 斜拖回退旧判定。② 对角机器夹角的孤格带 / 管：出料口外格也算拓扑
  （按介质匹配口），进色条 / tooltip / 弯道弧从机器口反推，不再是默认直箭头。
  实现注记：`LportDir` 的 u/d 与色条体系 t/b 是两套方向命名，`LOGI_OPP['u']` 为 undefined，已转译
- **v108 从机器口（红圈）起手拉带子** —— 按在机器朝外的输出口格上起手，带子从**口外那一格**开始铺、不占口格。
  旧版起手格被机器占着时 `Lfree` 判定失败，压根进不了连铺，「从红圈起手」做不到。
  `LsnapStart` 做成**纯探测**（只返回坐标、不改拖拽态，避免探测污染状态）；整段手势仍只压一次撤销栈。口外被占则不误起手
- **v109 机器口点击分流 + 协议核心出货选择** ——
  ① **点击分流**：手拿物流件按在机器口格上不再直接进 move，而是先只选中进 `portpend` 待决态；
  **原地松手 = 从口外起手连铺**（v108 那条路终于可达）、**拖过 4px 才转 move 移机器**。
  旧版命中机器就往 move 走，想从口红圈拉线一动就把机器拖走 —— v108 的「口外起手」分支永远走不到。
  ② **协议核心出货**：`sp_hub_1` 6 个出料口在**格内**画朝外的可点箭头 `.lo-dlv`，点开列出该核心所属域的可出货物品。
  数据源是 `raw/FactoryItemTable.json` 的 `deliverItemTypeList`（非空 = 可出货，值 `[3,1]`/`[3,2]` 的 1/2 是目标域序号）
  → 注入成 `DB.hubItems` **281 件**（四号谷地 70 / 武陵 211）；缺名的 19 件（掉落物 / 测试件）
  从 `I18nTextTable_CN.json` 按 `name.id` 反查补齐，0 件退 id。选中后面板内的箭头变绿并标出物品名，
  **再点同类 = 取消**；出货记录独立于摆放快照（撤销不影响）。
  ⚠️ `FactoryHubCraftTable.json` 是「造建筑」配方、与出货无关（已排除，勿再当数据源）。
  实现注记：箭头坐标按「格中心 + 内推」两版都越界（9×9 核心在 20px 格下左口格中心仅 10px，left 算出 -1 / -7）
  → 最终**压在口格中心、箭头缩到 12px**；域判定首版错用 `DB.bases.areas`（只有 4 条且**没有 levelId**）
  导致武陵基地永远匹配不上，被新增断言当场抓到 → 改用 `Lbases()`

### 1.4 ③ 单基地「摆得更紧、走得更短」（v50~v63）

- **Dijkstra 转弯罚寻路** —— BFS → 代价寻路（状态含方向、换向 +2），路径又短又直
- **参数搜索 + 按分择优** —— 间{4,3,2} × 对齐{关,开} × 通道{5,3} 12 组全摆打分自动选优，消息报搜索结果
- **桥接器**（参照 IndustrialPlanner Connector 规则）—— 已铺线格允许「正交直穿」，
  穿越格自动铺物流桥 / 管道桥（桥上不转弯、一格桥只跨一条线）
- **就近分组** —— 汇流组按 x 就近配对；失败 warn 带坐标；失败驱动 corr 爬升
- **层内主动换行**（v63）—— 上游比下游宽时（12 台块机 vs 6 台罐机），
  机器按「消费它的下游」分组、**组的 x 起点对齐下游机器**，与行内冲突就下沉一行（自动交错分行）
- **实测**：铁制零件@10 管线 10→6 格（-40%）；赤铜耐压罐@10 **全连通零手动连**；
  赤铜耐压罐@30 **手动连 15 → 0~1**、高度 90→54、线长再降 25%（1149→863 格）；
  生成耗时 0.3~2.6s（参数搜索 4~15 组择优）

### 1.5 试摆画布 · 拉线交互（v56，对齐游戏手感）

- **L 形拉线**：选中传送带 / 管道后拖拽，路径跟手势走（主轴先走、再转第二轴），弯头朝向自动衔接 —— 解决「拐不了弯」；管道同理
- **端点吸附**：起手 / 落点压在机器上时自动吸到「输出口 / 输入口」外一格（游戏里「出口旁边那格开始拉」）
- **弯道圆弧渲染**：弯道格画成 L 形圆弧带（进边 → 圆角 → 出边 + 出口小箭头），直线格保持箭头 —— 渲染时由流向表反推每格进边（v57）
- **分 / 汇流器替换线上段**：拿分 / 汇流器点已有的同类介质物流段 → 删段放分 / 汇（游戏同款替换；跨介质、压机器仍拒绝）（v58）
- **物流桥叠线 / 准入口串线**：桥点带 / 管格 → 叠上（原线保留）；准入口 → 替换那段（v59）

### 1.6 ⑤ 交互与可靠性

- **局部锁定**（v64）—— 锁 =「这台我满意了，别动它」：
  - **交互保护**：锁定件**拖不动 / 删不掉 / 转不了 / 不参与复制**，双击也不会误删；选中后按 `L` 锁定 / 解锁，工具栏有「锁定选中」「解锁全部」
  - **重排约束**：「重排其余」把锁定机器当**固定件**（位置一格不动），其余机器重新分层摆位并**绕开**它们，管线整条重铺；
    台数口径 = 节点台数 − 已锁台数（**机器总数守恒、产量不变**），手摆的散件留原地并被当障碍
- **分流器分支的真实工况覆盖**（v65）—— 扫全库找到真能触发它的三个目标：
  **工业爆炸物 / 实验玉铜发散器 / 息壤玉葫芦**（结构都是「1 台高产能上游要喂 5~10 台下游」）。顺带抓到两个真 bug：
  ① **一台分流器只有 3 个出料格，旧版只摆一个** → 第 4 台下游起**静默丢弃**（既不接线也不报警）；
  现改为**每 3 台下游并排摆一个**（各占上游 1 个出料口）；
  ② 「需要几个分流器」原来按「缺几个出料口」算，量级不对 → 改成 `ceil(下游台数 / 3)`。
  被丢下的线一律**逐条点名**；新增 `route.stats = {split, merge, dropped}`，生成消息报「汇流 X / 分流 Y」。
  实测：息壤玉葫芦@5 **分流器 2 个 → 5 台下游全接上、零手动连**
- **高产能 / 宽链的连通率**（v66）—— 先扫全库 52 个中大型工况量出短板（零手动连 44/52、手动连共 23 条），
  再逐候选拆分数找根因，两个：
  ① **打分口径反了**：2 条手动连只罚 200 分，盖不过 300 格线长差 → 「全连通但线长」的方案被判输。
  手动连权重 **100 → 400**（少一条线是玩家得动手补的功能缺陷，多铺格子只是效率问题）。
  ② **参数网格太窄**：机器间距只到 4，而宽链要「**间 8**」机器之间那条竖缝才够并行走线 →
  加「**失败驱动宽间距扩搜**」（只在还有手动连时才补跑，快路径不花额外开销）+ 汇流器两处口径修正。
  **效果：零手动连 44 → 51/52 例，手动连总数 23 → 1 条**；耗时 3.4~4.1s（只在难例上多花 1~2s）

### 1.7 数据修正（非功能项，但影响排布器读数）

- **矿机的出料口「用不上」，示意图不再画**（v68 / v70 / v71）：
  配置表里矿机挂着 3 个传送带出料口，但**两种模式都用不上**（默认无线回传仓库；切「仓储模式」也只是进缓存区、**手动取出**）
  → 画出来只会让人以为要接带子。现改为在建物上标 **⇡ 无线回传仓库**（电驱 / 二型电驱 / **水驱**）
  或 **⇥ 缓存区 · 手动收取**（便携源石矿机），水驱的**管道进料口**（清水）照常画；`ports` 数据保留、只是不渲染。
  两条更正：① **水驱矿机也是无线回传**（实机确认 —— 配置表里没有该字段，"字段缺失 ≠ 功能不存在"）；
  ② **水驱矿机是 1.1「新潮起，故渊离」（武陵工业二期）新增的，不是 1.5**（联网核实官方公告）。
  `mining_power.json → gather[]` 新增 `wireless` / `wirelessSource` / `msTransferCD` / `wirelessNote` 四个字段
- **免电变体（`_nop_`）连修带剔**（v92 / v93）：
  ① **精炼炉免电版耗电错标修正** —— 原始表 `powerConsume=5` 却缺 `needPower` 标志位，旧版误显「无需通电」，
  规整为 `powerConsume>0` 一律按耗电处理（标志位缺失 ≠ 不耗电，全表仅此一台）；
  ② **考据实锤**：6 台 `_nop_` 变体全部没有建造配方（67 条零命中），是剧情 / 系统内部建筑，建造菜单造不出来；
  ③ **v93 处置**：蓝图页剔除储液罐免电版（与普通版卡面一字不差、纯撞卡），其余 5 台保留（供电行可区分）。
  蓝图条目 107 → **106**，建筑设施页 107 不动

### 1.8 参照调研

- **IndustrialPlanner 源码规则提炼** —— 分 / 汇 / 桥生成规则、round-robin 轮转语义、五阶段仿真节奏，
  详见《IP 源码规则提炼.md》；桥接器与 round-robin 标注已照此落地

---

## 二、收官项与明确不做

### 2.1 ③ 可选尾巴（已收 —— v91）

- **按层级换行（跨料交错的地基）**：down 模式组内机器**到右墙折行**（回组起点、行下移继续摆）、冲突失败换行重试 ——
  旧版撞墙 `break` **静默丢机器**：赤铜耐压罐@30 只摆 19/30 台且报告零警告，赤铜块@10 丢 2 台后同样如此。
  修复后罐@30 **30/30 全摆、手动连 0**；组间同行穿插（跨料交错）由格级冲突检测天然支持，电池链已验到跨料同行

### 2.2 ⑤ 交互与可靠性（已全部收官 —— v64~v66）

见 §1.6。

### 2.3 ④ 原料侧闭环（已收 —— 能做的做完了，两项明确不做）

复核结论：**不按原方案做**。

**已落地**：水驱矿机的**供水 / 供电配比清单**（口径：**1 台水泵最多带 3 台水驱矿机满效率**）——
泵台数 = `⌈矿机数 ÷ 3⌉`、前 N−1 台各带 3 台 / 最后一台带余数、**每台泵 1 条管道就够**（60 < 管道上限 120）、
水泵要通电 10 电 / 台（水驱矿机靠清水自供能、不耗电）。报告里直接给。
（原料需求 → 要几台矿机 / 哪个区够 / 占用比例 早已在报。）

**明确不做**（原因不是"卡数据"，是**问题本身不成立**）：

- ~~矿点逐点坐标~~ → 三种矿机产物都是**无线回传仓库 / 缓存区手动取**，**野外没有需要连的线**，
  每个矿点独立放一台就完事 —— 没有连线 / 避让 / 优化，也就没有"排布问题"，坐标对排布器没有增益
  （只对"打开地图照着放"的清单有用，那不是这个工具干的事）。
- ~~供水管网走线~~ → **野外不是网格**，配置表里也没有地形与障碍（只有单段线长上限：供电桩 30m / 中继器 80m），
  水管怎么绕、电从哪拉只能看地形；能算的只有配比与数量，已给出。

### 2.4 ⑥ 多基地 / 全区域（三刀全落）

**⑥-1 跨地区收货**（v73 起，工具栏开关，默认关）：

开启后，链里的原料只要「能被跨地区传输」（物品表 `transferDomainIds` 那 243 件），就按
**四号谷地超库存传输过来**处理，本地**不建它的产线、不算它的矿机配比**。

- **机器台数与配方不变**（收货只改"料从哪来"）；收货的料仍留在原料清单里
- 报告给「需多少/分 · 单位物品价值 · 每批间隔」，并把界面上的**传输总值**填进来才反推
  「每批能带多少 / 供货速率多少」—— **每档具体多少配置表里没有**（`DomainDataTable` 的建设等级效果
  只有 bandwidth / battleBuildingLimit / travelPoleLimit / isMineOutputUp），所以不填就不给数字
  （v75 联网回填实锤：**11 级 1200 / 12 级满级 1500**，NGA×2 + GameRant 多源一致）
- **配套修掉一处自相矛盾**：原来「原料野外上限」和「原料侧闭环」还在按本地矿脉算
  「全图上限 / 要 1 台矿机」—— 同一份报告里既写着"跨地区收货"又写着"去野外摆矿机"。
  现在统一标 **本地不采、不计野外上限、不配矿机**
- **v74 真机回归修掉一处单位错**：供货速率少除 60（「个/小时」标成了「/分」），「喂不饱」红字永不触发。
  教训：期望值必须独立推单位，不能照实现抄
- **v75 联网核实补充**：解锁等级（**8 级**库存传输 / **12 级**超库存，GameWith+game735 双源；
  smzdm 孤源写 11）、档位（**1200 / 1500**）、**升 12 级后协议不自动切换**要手动改（NGA+smzdm 双源）、
  取消重设重置 1 小时计时（GameRant 单源）、传输在线才推进（NGA 单源，待实机确认）—— 详见 README §六
- **v79 单选修正**：「布局试摆里跨地区传输没给选择传什么物品的选项」→ 查实是 v73 简化错了。
  一条传输路线**一次只能传一种物品**（协议管理里 Edit → 选一种 → 启动；换物品 = 停止重设、计时重置回 1 小时；
  GameRant / GameWith / Game8 三源一致）——「能传的自动全收」**不成立**。
  改成：链里可传原料 ≥2 种时**只挑一种**走传输（默认**需求最大的**，最值得省的产能），其余回退**本地自产**；
  报告里出**选择器**随时换选（单候选不显示）。
  真机：中容谷地电池@1（蓝铁矿 10/分 + 源矿 15/分 双可传）→ 默认收源矿，换选蓝铁矿后源矿回退「矿机 20/分」
- **v80 选货网格**：原生 `<select>` 换成**候选卡片网格** —— 每卡：稀有度色条（`rarity` 1灰/2绿/3蓝/4紫/5金/6红）
  + 物品名 + 需求/分 · 单位价值 · 每批能带几个，选中打勾，**点卡即重算**；单候选也渲染选中卡，标题只在 ≥2 候选时出现。
  本库离线无图标资源 —— 色条与勾都是真实数据 / 状态，不造假图标、不引外部图片。
  **顺藤修掉 v73 起的「假开关」**：点收货开关原来只改文案不重算（报告挂旧 plan）→ 现改为开关即重算，
  与 LshipPick 同款，关掉同时清选状态
- **v81 链上全物品可选 + 面板即选**：
  ① 候选不再限定原料叶 —— 链上任何能传的物品都能选（半成品卡上标「半成品」；选中半成品 = 它的整棵上游子树
  不用建，引擎本来就支持：收货物被剔除出配方表 → 落原料并标收货）。实测电池链 7 件全可传，换选铁制零件后
  蓝铁块 / 蓝铁矿的机器全消失；目标本身不在候选里。② 选货条提到**产线面板开关正下方**，开关一开立刻能选
  （没生成产线也显示 —— 轻量展开算候选，不摆机器不动画布）；有产线时面板让位给报告（不出现两个网格）。
  换选补 plan 守卫：没生成产线时点卡只换选，不悄悄生成产线覆盖手摆画布
- **v82 全量可传清单 + 折叠搜索**：
  联网三源核实（Game8 / GameWith / Game8）：游戏里协议管理的候选 = **出发地能产出的全部物品**（解锁过产能就行，
  仓库有没有无所谓；武陵特产传不了）—— v81 的「链上可传」只是子集。候选扩成 链上可传 ∪ 全库「有机器配方
  且能送到」162 件 + 链上原料叶单独补（矿机产出也算地区产能）；排序：链缺原料 → 链上半成品（需求降序）→
  其余全量（名称序）收进**折叠区**带搜索框（160+ 件全平铺会撑爆面板）；**换目标立刻重算候选**；
  链外选中报告明说「只进仓库、产线不变」（明细 0 行不至于像没生效）。
  真机：平铺 5 + 折叠 158 → 搜索 158→25 → 链外选中生成（机器与基线一致 + 提示）→ 换目标候选即刷

**⑥-2 多目标共享中间产物**（v76，工具栏「＋ 目标」，最多加 3 个附加目标）：

主目标之外再填目标与速率，**所有目标放进同一张需求 DAG** —— 共用中间料只建一套产线，
出料再分流给各目标，不用为每个目标把同一条上游链各摆一遍。

- 实现：`Rexplode` 增加 `opt.seeds`（多种子）+ 按物品 memo 去重；中间料第一次被需要时正常建产线，
  之后每个再要它的地方挂一条**幽灵边**（引用同一节点、记着「谁在要」）；首个出现处也包成幽灵边，
  所有消费边同构 —— 分流器 / 合并器、按消费者的管线统计直接复用 ⑤-2 机制，`RwRoute` **零改动**
- 合并需求统一重算：`RexplodeFinal` 按 Kahn 拓扑把「父节点整台实出 × 配比」灌回共享节点，
  台数 = ⌈合并需求 / 单机速率⌉；共享节点深度取**最深消费者 + 1**；两轮推演（原始 / 带进货）口径不变
- 报告：头部目标从 1 个变 N 个（「 ＋ 」连接）；新增**共用中间料**段（几台 · 合并需求 · 整台实出 → 分流给谁多少/分）；
  原料清单取各目标并集；共用节点在管线明细里按消费者各给一行
- **口径提醒：合并不保证省机器** —— 整台进位不重合时合并台数 = 分开建之和，
  省的是**同一套上游产线只摆一遍**：一条矿线、一套水线，出料再分流
- 实测：赤铜耐压罐@10 ＋ 赤铜瓶@10 → 共用赤铜块 8 台（合并需求 40/分）、共用清水 8 台；
  ⚠️ 这种双目标长链默认 50×50 画布放不下（守卫提示「试了 N 组参数都越界」是**按设计触发**），换 80×80 即可

**跨基地选点**（v83，**分配建议器** —— v1 只出建议不摆画布）：

报告尾部常驻「哪个成品放哪片基地更省」，布局面板「选点建议」开关同款。收货方向改「从（）到（）」双下拉
（清单动态读 domains，未来新地区零改动接入）；两地对称互传 —— 单端下拉选另一端 = 换向；
切基地时「到」自动对齐所在地区。成本 = 2^N 组合穷举：硬否决 > 同方向多种收货物 > 口径①喂不饱 >
分两地重复建共享料 > 矿缺口量。两口径都入模：
① 地区合计需求对每批可到货反推（罐@30 全放谷地 → 赤铜矿缺 60/分 vs 供货 25/分 → **喂不饱**）；
② 取货口 3×1×3、路数 =（边长−1）÷3（谷地 23/13 实测、武陵 26/16 推算），吞吐无单列数据 → 明说不编。
「不能跨地区传输 + 产线未建模」的料（惰性壤晶废液 80/分、惰气 10/分）**点名给量不静默**。
现有单链公式（台数 / 收货反推 / 摆位）**不受粒度影响**，一行没改。

- ⚠️ **硬约束**：集成工业系统**同时只在至多两个地区运行**；
  仓库分地区且互相独立，**一地区一个仓库、区内几片基地共享** —— 收发粒度是「地区」不是「基地」；
  跨地区传输**每批只能传一种物品**，数量上限 = 传输总值 ÷ 单位物品价值；
  批次间隔 `FactoryConst.domainTransportIntervalTime = 3600` → ⑥ 的「多基地」实际是**两地联动**。
  **工具口径**：只按**超库存传输**建模、按**满级**取传输总值 **1500/批**，11 级 1200 档不进工具

---

## 三、当前状态

**③ 单基地优化全部收官**（赤铜耐压罐@30 手动连清零、线长 -25%），
**⑤ 交互与可靠性也全部收官**（局部锁定 v64 · 分流器分支 v65 · 高产能连通率 v66 —— 全库 52 个中大型工况里 **51 个零手动连**），
**④ 也已收口**（配比清单给了，两项明确不做），
**⑥ 三刀全部收官**（跨地区收货 v73-v82 —— 原料能从对面地区超库存传输过来，一条路线一次只传一种；
多目标共享中间产物 v76 —— 共用中间料只建一套再分流；跨基地选点建议器 v83 —— 方向双下拉 + 对称互传 +
2^N 组合穷举给「哪个成品放哪片基地更省」，两口径全入模）。
**③ 最后一条可选尾巴也已收**（v91 按层级换行）。

⚠️ v91 顺带纠偏：⑤-3 的「51/52 零手动连」曾含假绿成分
（部分工况靠 down 候选静默丢机器清零）—— 修复后按「全摆优先」复验：罐@30 真清零（30/30 · 手动连 0），
赤铜块@10 是 12/12 全摆 + 手动连 2（诚实提示，不再用丢机器换假 0）。**全部条目收官，无剩余尾巴。**

收官后新增对齐游戏的功能：**v103 气体散布机环境圈**（13×13 方形 · 四气四色 · 可换气）。

> ⚠️ **一处真机才会触发的 bug 修复**：`RwRoute` 里 `const outNeed` 写在了 `if` 块内、却在 `else if` 里引用 ——
> 老测试把 `const` 换成 `var` 跑，块级作用域错误被**掩盖**，浏览器里一跑 `LawRun` 就 `outNeed is not defined`（排布器整个点不动）。
> 现在 `test_html.js` 多了一道「**用原始代码（const/let 版）冒烟跑关键路径**」的门禁守着这类问题。

---

## 四、回归测试

所有功能改动都带回归断言。以下为各阶段的验收规模（`test_html.js` 日常档，另含 `test_layout_events.js` 事件层）：

| 版本 | test_html | 事件层 | 增量说明 |
|---|---|---|---|
| v64 局部锁定 | 553 | 72 | +33 锁定保护 / fixed 避让 / 重排守恒 |
| v65 分流器分支 | 565 | 72 | +11 含大产线「摆不下如实报数」 |
| v66 连通率 | 572 | 72 | 打分权重与参数网格修正 |
| v73 跨地区收货 | 600 | 72 | +15 数据层 / 判定 / 开关对照 / 间隔常量 |
| v76 多目标共享 | 620 | 72 | +20 共享节点 / 幽灵边 / 需求合并 / ⑥-2×⑥-1 组合 |
| v79 单选修正 | 626 | 72 | +4 单选（另 v77 预填 +2） |
| v80 选货网格 | 628 | 72 | +2 开关即重算 |
| v81 链上全物品 | 631 | 72 | +3 面板即选 |
| v82 全量清单 | 633 | 72 | +2 折叠搜索 |
| v83 跨基地选点 | 667 | 72 | +34 组合穷举 / 两口径 |
| v91 按层级换行 | 668 | — | 两条假绿断言改「全摆优先」真口径 + smoke 同步 |
| v103 环境圈 | 681 | — | +18 注入 / 摆放 / 渲染 / 换气 / 开关 |
| v104 环境三连 | 692 | — | +11 四色校准 / 就地选气 / 依赖透明化 |
| v105 选气条误摆 | 692 | 75 | +2 事件层回归 |
| v106 一格拐弯 | 700 | 75 | +8 拓扑口径 |
| v107 拐弯跟手势 | 706 | 78 | +6 / +3 轨迹与机器口反推 |
| v108 口外起手 | 710 | 81 | +4 / +3 纯探测与口外交接 |
| v109 口上点击分流 + 核心出货 | **732** | **103** | +22 / +13；HEAVY 全量 746 |

> **门禁口径**：日常档 732 项必须全绿、`fail=0` 才允许发布；
> `HEAVY=1` 跑全量（含被跳过的 14 条大链用例）共 746 项。


## 五、README 迁入的路线图与版本日志（2026-09-24）

> README 门面化重构时迁入：原 README「物流规则」下的路线图各节（③④⑤⑥ 系）与散落的版本更新段（v73~v122），
> 原文未改，仅整段搬移。文中「详见 XX 一节」若指 README 的章节，请回 [README](README.md) 或 docs/ 对应手册查看；
> 各条目里的验收数字是**当时版本**的基线，不代表当前（当前基线见 docs/维护与更新.md）。

### 旧修订记录引用块（README L31-37，2026-09-24 迁入）

> **2026-09-19 修订**：本轮做了一次代码审计 + 精简，修掉 6 个 bug（详见文末「审计与回归」），并新增回归测试脚本 `tools/test_html.js`。筛选器的候选项现在**全部由数据推导**，不再手写。
>
> **2026-09-20 新增**：新增「基地面积」页 —— 各基地建设区域多大、能放几张蓝图、扩建要多少券、据点发展等级给多少建造上限。
> 表格统一按 **满级 / 面积最大值** 口径（据点发展等级满级 + 区域扩大·二），**每片基地一行**（每个据点 1 主 + 3 副，共 8 片）。
> 四号谷地主基地 = 枢纽区，武陵主基地 = 武陵城；其余 15−8=7 个建造区没有基地。
> ⚠️ **这一页里面积是社区实测，不是配置表数据**，来源和可信度都标在页面上，引用时请分开说明。详见「基地面积与建造上限」一节。

---

### 单基地优化（路线图 ③ 第一梯队 · v50）

- **走线**：BFS 升级为 **Dijkstra（状态含方向，转弯罚 +2）**——路径又短又直；管线「由短到长」铺（流体优先，防被带子截断）
- **参数搜索 + 按分择优**（v52）：间{4,3,2} × 按列对齐{关,开} × 通道{5,3} 共 12 组全摆一遍打分，
  按「连通段 > 手动连 > 总线长」选高者，生成消息报搜索结果。实测铁制零件@10：管线 10→6 格（-40%）
- **层内主动换行**（v63）：上游比下游宽时（12 台块机 vs 6 台罐机），机器按下游分组、
  组的 x 对齐下游机器、冲突下沉一行——实测赤铜耐压罐@30 手动连 **15 → 0**、高度 90→54、线长再降 25%
- **桥接器**（v53，参照 IndustrialPlanner Connector 规则）：已铺线格允许「正交直穿」，
  穿越格自动铺**物流桥 / 管道桥**（桥上不转弯、一格桥只跨一条线）。
  实测赤铜耐压罐@10：2 条被围死的管道全部救回（手动连 2→0、全连通）
- **分流器语义标注**：1 进 3 出是 round-robin 轮询均分（参照 IndustrialPlanner 仿真内核），不是按需分配

### 试摆画布 · 拉线交互（v56）

- 选中传送带/管道后在画布**拖拽拉线**：路径跟手势走（L 形拐弯，弯头朝向自动衔接），管道同理
- **端点吸附**：起手/落点压在机器上时自动吸到「输出口/输入口」外一格，带子贴着机器铺
- **弯道圆弧渲染**（v57）：弯道格从「独立箭头」升级为 **L 形圆弧带**（进边 → 圆角 → 出边 + 出口小箭头），
  和游戏里的弯道一个观感；直线格保持箭头
- **分/汇流器替换线上段**（v58）：拿分/汇流器点已有的传送带/管道格 → 删掉那段、放分/汇流器（游戏同款）；
  跨介质与压机器依然拒绝；方向不对按 R 旋转
- **物流桥叠线 / 准入口串线**（v59）：拿物流桥点带/管格 → **叠上**（跨线，原线保留）；
  拿准入口点带/管格 → **替换**那段（串进线里）；拖动已摆的桥/准入口同理

### 局部锁定（路线图 ⑤-1 · v64）

「锁住满意的机器，只重排其余」—— 手调好的那几台，再也不会被下一次生成冲掉。

- **怎么锁**：选中件 → 工具栏「**锁定选中**」（或按 **`L`**）→ 变成**双线铁灰 + 右上角小锁**；
  「解锁选中」精确解锁、 「解锁全部」一键放开（都进撤销栈）
- **交互保护**：锁定件**拖不动 / 删不掉 / 转不了 / 不参与复制**，双击也不会误删；
  锁定件（含手拉的线）不会被分/汇流器替换或叠桥
- **重排其余**：排布器那一栏的按钮 —— 锁定机器当**固定件**（位置一格不动），
  其余机器重新分层摆位并**绕开**它们，管线整条重铺
- **口径**：交给布局的台数 = 树节点台数 **−** 已锁台数，锁定件自己拼回摆放列表
  → **机器总数守恒、产量口径不变**；画布上不归排布器管的散件原样留在原地，并被当障碍避开
- **已知边界**：锁定只对**机器**有意义 —— 管线是算出来的产物，重排时一定重铺（按钮与提示都写明了）

### 分流器：按「每 3 台下游一个」并排摆（路线图 ⑤-2 · v65）

分流器是 **1 进 3 出** —— 一台上游机器要喂 5 台下游，就得摆 **2 个**（各占上游一个出料口）。

- **以前只摆一个**：第 4 台下游起**既不接线也不报警**，报告里还写着「手动连 0 条」，看着像全连上了
  （实测「工业爆炸物@5」5 台下游只连上 3 台）。现在**每 3 台下游并排摆一个**分流器。
- **口径修正**：提示里的「需要几个分流器」原来按「缺几个出料口」算（`outNeed − outCap`），
  量级不对 → 现在按 `ceil(下游台数 / 3)`。
- **不再静默丢**：新增 `route.stats = {split, merge, dropped}`；凡是有线没连上，报告都会**逐条点名**
  （「还有 N 台下游没连上」），生成消息里也报实际摆放数「汇流 X / 分流 Y」。
- **真实触发工况**（扫全库找出来的）：**工业爆炸物 / 实验玉铜发散器 / 息壤玉葫芦** —— 都是
  「1 台高产能上游喂 5~10 台下游」。实测**息壤玉葫芦@5：分流器 2 个 → 5 台下游全接上、零手动连**。

### 高产能 / 宽链的连通率（路线图 ⑤-3 · v66）

先扫全库 52 个中大型工况量出短板，再逐个候选拆分数找根因 —— 两个：

| 根因 | 修法 |
|---|---|
| **打分口径反了**：2 条手动连只罚 200 分，盖不过 300 格线长差 → 「全连通但线长」被判输 | 手动连权重 **100 → 400**（少一条线是玩家得动手补的功能缺陷，多铺格子只是效率问题） |
| **参数网格太窄**：机器间距只到 4，而宽链要 **间 8** 竖缝才够并行走线 | 加「**失败驱动宽间距扩搜**」——只在最优方案还有手动连时才补跑，快路径一分钱不花 |
| 汇流器 **fanin=3** 没算进 T；下游口不够时还死守「省 ≥4 才并」 | 并线时把 T 抬到 `⌈N/3⌉`；**下游口不够 = 并线是"必须"不是"划算"** |

**效果**：全库 52 个中大型工况里**零手动连 51 个**（改前 44），手动连总数 **23 → 1 条**。
赤铜耐压罐@30 **手动连 0**（且改用「间8/分层对齐」后**不再需要汇流器**，线 463 格）；液化息壤@10（5 依赖混合链）从 6 条压到 1 条。
耗时：普通链不变，难例多花 1~2s（3.4~4.1s）。

### 原料侧闭环（排布器报告里）

报告现在会把「这条产线需要多少原料」推成野外侧的动作：

- 需 **几台矿机**（需求 ÷ 20/分，向上取整）+ 用的是哪种矿机
- **哪个区的矿点够**（按点数降序列出可选区 + 合计够不够 + 占用比例）
- 水驱矿机 → **耗水 20/分·台** → 需几台水泵（60/分·台，1 泵最多带 3 台）

**水与电的配比清单**（2026-09-22 补，口径：**1 台水泵最多带 3 台水驱矿机满效率**）：

- 水驱矿机台数 × 20/分 = 总需水 → 泵台数 = `⌈台数 ÷ 3⌉`（水泵 60/分·台）
- **分管建议**：前 N−1 台泵各带 3 台、最后一台带余数；**每台泵 60/分 < 管道上限 120/分 → 每台泵 1 条管道就够**，不用并联
- **供电**：水泵要通电（10 电/台）；水驱矿机**靠清水自供能、不耗电**；电驱矿机 5/10 电/台（野外要拉电或就近放供电桩）

### ④ 的边界：野外段的「摆放与走线」**不做**（不是遗漏）

2026-09-22 复核「那这样 ④ 还用做吗」—— 结论是**不用按原样做**，两条原因：

1. **野外没有需要连的线**：三种矿机的产物都是**无线回传仓库**（水驱 / 电驱 / 二型电驱）或**缓存区手动取**（便携源石矿机），
   每个矿点独立放一台就完事 —— 没有连线、没有避让、没有优化空间，也就没有"排布问题"。
2. **野外不是网格**，配置表里也没有地形与障碍（只有单段线长上限：供电桩 `autoConnectLength` **30m** / 中继器 **80m**）——
   水管怎么绕、电从哪拉，只能看那一片地形长什么样。

⇒ 路线图 ④ 的「**矿点逐点坐标**」与「**供水管网走线**」在本工具里**标为不做**；能做的**配比清单**（上面那三条）已经给出。

### 滑索只放野外（2026-09-21 定）

**滑索架不摆进基地画布，只放野外** —— 所以布局试摆的左栏清单里没有滑索架（`LO_SKIP_IDS` 里拉黑），
建造区的 `travelPoleLimit`（枢纽区 20 / 谷地通道 10 …）约束的是**野外那一层**，与基地内的产线无关。
滑索架射程 80m、长距滑索架 110m（详见「滑索射程」一节）。
### 跨地区收货（路线图 ⑥-1 · v73 / v79 单选 / v80 选货网格 / v81 链上全物品可选 / v82 全量可传清单 —— ⑥ 的第一刀）

布局页工具栏多了一个开关「**跨地区收货**」（也可 `LO.shipIn`）。**默认关**，开着时才改口径。

做的事只有一件：**把这条链的原料里「能被跨地区传输的那些」，从"本地自己产 / 野外自己采"改成"由四号谷地超库存传输过来"**，
本地**不再给它建产线、不再算它的矿机配比**。

- **判定**：这个料在 `FactoryItemTable.transferDomainIds` 里（→ `data/items.json` 的 `domains` 非空）就能送到。
  野外那些气体 / 废液（惰气、惰性壤晶废液）**不在清单里** → 传不了，照旧按野外开采处理。
  用「赤铜耐压罐@10」试：原料从 3 项变成赤铜矿收货 + 惰气 / 惰性壤晶废液照旧。
- **不误伤**：**机器台数与配方完全不变**（收货只改"料从哪来"）；收货的料**仍留在原料清单里**，不会被静默抹掉。
- **工具口径（2026-09-22 定）**：只按**超库存传输**建模、按**满级状态**取传输总值 **1500/批**
  （11 级 1200 档只作资料记录、不进工具）；仓库粒度按「地区」—— 收来的货进该地区仓库，区内几片基地共享。
  **粒度不影响现有公式（2026-09-22 核对）**：机器台数、收货反推（每批 = 传输总值 ÷ 价值、速率 = 每批 ÷ 间隔）
  本来就是「地区仓库」口径 —— 传输总值在协议管理里就是出发地地区级的数字，收货方是几个基地共用一个仓不改变算式。
  会受影响的是 ⑥-3 的两件事：同区多基地要**同一种收货物**时，喂不饱判定得按「地区合计需求」对传输总值算；
  以及「地区仓库取货口 → 各基地产线」那段要入模（取货口 3×1×3 有占格数据，吞吐没有单列数据）。
- **只挑一种走传输（v79 单选 —— ⑥-1 的修正）**：v73 原实现「能传的自动全收」是**错的** ——
  一条传输路线**一次只能传一种物品**（协议管理里 Edit → 选一种 → 启动；换物品 = 停止重设、计时重置回 1 小时，
  GameRant / GameWith / Game8 三源一致）。链里有 ≥2 种可传原料时**只挑一种**走传输（默认挑**需求最大的** —— 最值得省的产能），
  其余回退**本地自产**（产线照建、矿机照配）。
  真机实测：中容谷地电池@1（蓝铁矿 10/分 + 源矿 15/分，两条原料叶都可传）→ 默认收源矿 15/分；
  换选蓝铁矿后，源矿回退「矿机 20/分」本地采矿。
- **选货网格（v80 —— 起因：游戏内传输界面有物品选择器，而试摆器缺这一环）**：
  原生 `<select>` 换成**候选卡片网格** —— 每张卡：稀有度色条（物品表 `rarity`，1灰/2绿/3蓝/4紫/5金/6红）+
  物品名 + 需求/分 · 单位价值 · 每批能带几个（tv 没填就不给数），选中卡打勾高亮，**点卡片即重算**。
  单候选也渲染一张选中卡（「这条链在传什么」一眼可见），标题与规则文案只在 ≥2 候选时出现。
  本库是离线单文件、没有图标资源 —— 色条与勾都是真实数据/状态，不造假图标、不引外部图片。
  **顺藤修掉一个 v73 起就有的「假开关」**：点「跨地区收货」开关原来只改文案不重算，
  报告还挂着旧 plan（要手动再点「生成产线」开关才真的生效）—— 现在开关即重算（与换选同款），
  关掉时同时清单选状态。真机：双卡渲染 → 点蓝铁矿卡即换选 → 点开关关掉后收货段与网格立即消失。
- **链上全物品可选 + 面板即选（v81 —— 起因：v80 的候选只列源矿与蓝铁矿，其余可传物品无法选中）**：
  两处产品级修正：
  ① **候选不再限定原料叶** —— 链上任何「能被传输」的物品都能选，半成品/中间件的卡上标「**半成品**」。
     选中半成品 = 它的**整棵上游子树**不用建（引擎本来就这么算：收货物在展开器里被剔除出配方表 →
     落进原料并标收货）。实测电池链 7 件物品全部可传：源矿 / 蓝铁矿（原料）+ 源石粉末 / 铁制零件 / 蓝铁块
     （半成品）全都能选；换选铁制零件后连蓝铁块、蓝铁矿的机器都不用建了。目标本身不在候选里（传目标 = 整条链消失）。
  ② **开关打开立刻能选** —— 选货条从报告里提到**产线面板开关正下方**（还没生成产线也显示：
     用轻量展开算候选，不摆机器、不动画布），选好再点「生成产线」；有产线时面板选货条**自动让位**给报告
     （同一份候选不出现两个网格）。换选也补了守卫：没生成产线时点卡片只换选，**不会**悄悄把产线生成出来
     （那会覆盖手工摆放的画布）。卡片上的需求数字取「收货前」的全链口径，换选前后不跳变。
  验收 628 → **631**（+3：半成品换选砍上游 / 需求口径稳定 / 面板即选）。
  真机：无产线开开关 → 选货条即在面板（画布 0 动）→ 点蓝铁块换选 → 点「生成产线」→ 收货 = 蓝铁块、
  其上游蓝铁矿消失、面板让位。
- **全量可传清单 + 折叠搜索 + 链外选中讲清楚（v82 —— 起因：候选仍只有两种，未覆盖全部可传物品）**：
  上网核实了游戏口径（Game8 / GameWith 三源一致）：**游戏里协议管理的候选 = 出发地（四号谷地）集成工业
  能产出的全部物品** —— 解锁过产能就行、仓库里有没有都行；只能传出发地能产的（武陵特产的西岚矿就不行）。
  所以 v81 的「链上可传」只是子集，这版把候选扩成**全量**：
  ① **候选 = 链上可传 ∪ 全库「有机器配方且能送到」的物品**（`RwMade ∩ RwCanReceive`，162 件），
     链上的野外原料叶（源矿/蓝铁矿 —— 矿机产出也算地区产能，游戏里能传但没有机器配方）单独补进来。
     排序：链缺的原料 → 链上的半成品（都按需求降序）→ 其余全量按名称；**换目标立刻重算候选**
     （「换了目标还挂着旧链的蓝铁矿」就是缺这一步），旧选中若仍是合法可传物品则保留。
  ② **折叠区**：全量 160+ 件全平铺会把面板撑爆 —— 链上候选平铺在前，其余收进 `<details>` 折叠区
     （「全部可传物品（N 件 —— 出发地能产且能送到的都在这）」），带**搜索框**（按物品名实时过滤，不重渲染）。
  ③ **链外选中讲清楚**：选了这条链用不上的东西，这条链的收货明细一行都没有、机器一台不变 ——
     报告会明说「选中的「X」**不在这条产线的链上** —— 传过来的货**只进仓库**」，不至于让人以为收货没生效；
     收货段条件随之放宽（开着收货且有候选就渲染，不再要求链上真有收货行），面板让位条件同步简化。
  验收 631 → **633**（+2：全量折叠网格 / 链外选中提示+换目标刷新，其中 2 条旧断言按 v82 语义改写）。
  真机：面板平铺 5 张链上卡 + 折叠 158 件 → 搜索「赤铜」158→25 件、清空复原 → 点折叠区「赤铜粉末」生成
  → 这条链不收货、机器 8 台与不收货基线一致、报告出「只进仓库」提示 → 换目标紫晶纤维链 → 候选即刷
  → 点「紫晶矿」生成 → 收货 = 紫晶矿、明细行正常。
  **v88 更新（下拉表即选）**：平铺区取消——链上原料/半成品
  不再单独立在外面，全部收进「全部可传物品」唯一折叠下拉（链缺的原料排最前），summary 常显当前选中；
  验收 667（3 条断言按 v88 语义改写：无平铺 / 折叠=全集 / summary 常显选中）。
  **v89 更新（瓶罐内容物可见）**：同名瓶罐变体（赤铜耐压罐×8 等）在选货卡上
  显示构建期反推的 content 标注（「装：水蒸气（气态）」橙色小字，空容器标「空容器（可灌装）」）。
  **v90 更新（传输口径改为「每小时一次性」）**：显示口径全改
  **每小时主显** —— 选货卡/产线报告/地区收货报告三处「约 X/分」换成「每小时可传 N 个（每批 M 个 ·
  H 小时/批）」，喂不饱警告也按小时折算对比（按每小时折算 N 个 &lt; 需求 M 个）；每分钟换算只留作内部判定。
  **v91 更新（③ 收尾：按层级换行 + 丢机器修复）**：down 模式层内换行的组内机器**到右墙折行**、
  冲突失败换行重试 —— 旧版撞墙直接 `break` **静默丢机器**（赤铜耐压罐@30 只摆 19/30 台、报告零警告，
  ⑤-3「手动连清零」部分是丢机器换来的假绿）；修复后 30/30 全摆、手动连 0（真机复核一致）。
  两条假绿断言改为「全摆优先」真口径（验收仍 668）；画布真放不下时现在诚实报「放不下」，不再出缺机器的图。
  **v92 更新（储液罐容量与耗电核对）**：修掉精炼炉免电版
  （`furnance_nop_1`）耗电错标——原始表 `powerConsume=5` 却没打 `needPower` 标志位，旧版把它误显成
  「无需通电」；现规整为 **`powerConsume>0` 一律按耗电处理**（标志位缺失 ≠ 不耗电，全表仅此一台漏标）。
  顺带考据实锤：6 台 `_nop_` 变体**全部没有建造配方**（67 条建造配方零命中，普通版全有），是剧情/系统
  内部建筑，建造菜单里造不出来——与实机观察吻合。
  **v93 更新（只剔除撞卡的，其余保留）**：占地蓝图页剔除**储液罐免电版**（`liquid_storager_nop_1`）
  ——它和普通储液罐卡面一字不差（普通版本就不耗电，蓝图表又没导出液体容量/协议容量），玩家也造不出
  免电版，纯属撞卡混淆；其余 5 台免电变体靠供电行可与原版区分，**保留**。蓝图条目 107 → **106**，
  建筑设施页 107 条不动。  用 ID 精确点名剔除，不做「撞卡就剔」的通用规则（避免将来新数据撞卡被静默吞掉）。
  **v94 更新（精简代码 + 提升编辑验证效率）**：先 profile 后动刀——
  实测 48.6s 全链耗时里 **97% 在 test_html.js**，且全部集中在**布局求解器**（⑤-3/⑥ 系列大链求解，
  单次 3~6s；render 与页面初始化合计 <0.5s，不是瓶颈）。三刀提质：① 同题断言共享一次求解
  （赤铜耐压罐@30 的「扩搜标记」不再重解一遍）；② 门禁节「大产线 30/分」换小链（分支可达性
  不必付 3s 求解成本）；③ 门禁节「⑥-3 换向」清 plan 残留 + 走 RxlHtml 空目标分支
  （完整渲染主节已验；有 plan 残留时 LshipDirV 会静默重算两次产线 = 白付 6s）。
  **test_html.js 48.6s → 36.7s（-24%），668+72 断言全绿，口径一根没动**。
  新增 `tools/release.py` 一条龙：build → build_html → 两个测试门禁 →（--push）推送终验，
  单命令出流水账。数据侧体检：JSON 已是紧凑格式（零缩进浪费）、items 字段 9/9 全在用 —— 无「白带体积」。
  下一步候选（未动工）：求解器层优化（参数搜索剪枝 / RxlBest 按参数 memo，真机选点面板同受益）。
  **v95 更新（低风险瘦身：注入层裁死字段）**：扫描五大表字段利用率 —— items 9/9 全在用、
  JSON 已紧凑零缩进浪费；唯 buildings 内联的接口原始明细（`inputPorts`/`outputPorts`/`inputEdges`/
  `outputEdges`/`edgeNames`）与 blueprint 条目的后三个是**死重**：页面消费的接口数据来自 blueprint
  的 `ports`（build.py 加工的精简版），原始明细属于数据底账。**五重验证零引用**（直接引用/动态拼接/
  Object.keys/for-in/测试断言）后在 build_html.py 注入层裁掉（data 文件保持完整，回滚=删一段），
  共裁 640 处，**HTML 1417 → 1356.5 KB（-60 KB）**，668+72 全绿。数据底账分层的边界从此明确：
  data/*.json 完整考据，页面 DB 只带运行时必需。
  **v96 更新（多目标计算 bug 修复：外部输入节点不再产「空气机器」）**：全库扫描 200 物品×2 速率×50/70 画布
  抓到 ⑥-2 专属坑 —— RexplodeFinal 对 external 节点（链深到顶按外部输入处理、本就没有机器）也跑
  `ceil(demand/1)` 凭空算出台数（息壤 10/分 → 报出 10 台「空气机器」），混进机器清单后报告虚报台数、
  画布上这些节点静默缺失。修复：external 节点台数恒 0、demand 照记（原料需求口径不变）。
  单目标路径不走 RexplodeFinal，所以 668 项回归从未炸过 —— 多目标专属坑。回归锁 +3 断言（无配方节点
  不进机器清单 / external 台数恒 0 / msg 台数与画布机器数一致），测试 671+72 全绿；全库重扫单目标
  323 工况 / 多目标 12 对 / 选点全量零 bug。产能与原料需求口径本就未受影响。
  **v97 更新（回归两档制 + README 发布条目工具化）**：test_html.js 加 HEAVY 开关：跳过 ⑤-2/⑤-3 六簇单次求解 >800ms 的大链走线用例（LawRun hook 实测 12 条合计 ~13.5s，走线 BFS 占回归总耗时约九成），⑥-2 多目标回归锁不跳；RESULT 带 skip=N 对账（659+12=671）—— README 条目从此固定锚点插入、三行短格式（tools/release_note.py 自动版本号与 HTML 体积）
  验证：日常档 27.2s pass=659 skip=12、HEAVY=1 全量 pass=671，test_layout_events.js 72 全绿；HTML 1357.3 KB。
  影响：日常回归 44.5 → 27.2s（-39%）；发布前须 HEAVY=1 全量跑；页面产物无变化（HTML 未重建）。
  **v98 更新（建筑专属限摆：天有洪炉 ≤12 台（⑥-4））**：经查证：天有洪炉摆放上限当前 12 台（武陵合计，1.2 工业计划 8→12、息壤工业科技分阶段解锁）；配置表 buildings.json hasPlaceLimit=false 不含「科技解锁型限摆」数据域（运行时系统数值同矿脉产率），故 RW_PLACE_LIMITS 手工维护、版本抬上限时改一处。口径=报警不拦截：按 machineId 对台数求和 >12 时 msg 点名超限并建议降速/收货；「放不下拒生成」分支同样带提示（超限链单层宽必超画布，几乎必然伴随放不下）
  验证：test 675+72 全绿（+4 断言：纯函数 @10 不误报/@70 点名、端到端轻/重），日常档 28.8s pass=662+skip=13、HEAVY=1 全量 675；HTML 1359.2 KB。
  影响：息壤链超限场景（膨地啪@70 需 28 台炉）msg 点名游戏限摆，玩家不再误判排布器；HTML 1358.8 KB。
  **v99 更新（层内换行行距修复：折行不再复用层间通道）**：层内折行行距原来是「机器高 + 层间通道 corr（最高 14）」——把跨层走线主干道的高度错用在了层内行间，超限大链三层折行总高 153 直接吃穿 80 画布、8 组候选全「放不下」。v99 行距解耦：rgCap 缺省 = corr（不爆的工况与旧行为逐格一致，零回归风险），纵向爆才 -2 递归降级、下限 3。超限链（膨地啪@60/70，24/28 台天有洪炉）从「直接拒生成」变为「摆得下 + msg 点名超限」——「报警不拦截」口径的延伸。
  验证：HEAVY=1 发布档 676 项全绿（675 + 新增膨地啪@30 满配 12 炉无超限误报端到端）；日常档 662 + skip 14 对账闭合；探针定标：赤铜块@10 / 液化息壤@10 宽行距工况逐格不变（压行距会退化手动连，回归抓过后改降级方案）；HTML 1361.3 KB。
  影响：build_html.py：LawPlan 行距参数 rgCap + 函数尾递归降级（LawRun/splitRun 零改动自动受益）；test_html.js：⑥-4 端到端断言更新（@70 翻转为摆下+点名超限、新增 @30 满配）；HTML 1361.3 KB。
  **v100 更新（画布补 80 收官：全库扫描器入库 + A5 跨组重叠修复）**：tools/scan_all.js 入库（五断言：异常/出口合法/台数守恒/空气机器/越界重叠）；首轮 80 画布全量 600 格抓出 6 格真重叠，根因=LawPlan down 分支 rows 只记机器起点行，跨组行距错相（炉 h3 行距 9 vs 提纯机 h5 行距 11）时 clash 判定 r.y===y 全盲；修为 y 向区间相交判定（rows 带 h，与 fixed 铺行口径统一）
  验证：日常档 pass=662 skip=14 零回归，HEAVY=1 全量 676，全库扫描 600 格 pass=600（生成 195/闸门 111/跳过 294），探针 6 格重叠清零；HTML 1361.7 KB。
  影响：6 个重叠目标布局修复且布线缩短（分离芯 -102、玉葫芦 -370、龙泡泡 -263 格，台数不变）；scan_all.js 为新增推送文件。
  **v102 更新（布局试摆帮助块收进下拉：整块黄区默认折叠）**：2026-09-23 反馈帮助块占地太大——整块 note 包成 details.lo-help 默认收起，summary 常显标题一行（复用 rl-ev 的 ▸/▾ 箭头样式），版本 tag 行挪进正文；顺手修掉版本记录段连续渲染两次的既有 bug
  验证：日常档 pass=662 skip=14、HEAVY=1 全量 676 全绿；生成 HTML 字符串级断言 details×1/summary×1/重复段×1；HTML 1361.9 KB。
  影响：页面：布局试摆 tab 首屏高度大幅缩短，帮助内容点标题展开；无脚本逻辑改动。版本号人为并档 v101→v102（v100 拆两 commit 错位一格，故并档；内容已随页面 commit v102 上线，本条目随下次发布入线上 README）。
  **v103 更新（气体散布机环境圈：13×13 方形 · 四气四色）**：2026-09-23 对齐游戏功能——游戏里散布机生效范围可见、圈色随气体变。数据链打通：FactoryVaporizerTable.rangeExtend{x:5,y:5,z:5} → 3×3 本体外扩 5 = 13×13 **方形**（实测确认非圆）；FactoryEnvDisplayTable GenEnv1-4 → 白/蓝/橙/绿（惰气/水蒸气/酸气/息壤气，游戏取色不可得用近似色）。实现：build 注入层把两张表灌进 DB.blueprint（同 ⭐content 通道）；渲染层 .lo-env 半透明色块+同色格线（CSS 变量传色），pointer-events:none 不挡点击，越界自动裁剪；LgasSet 批量换气换色、圈层开关独立于接口开关；帮助手册补💨区块
  验证：681/681 全绿（新增 18 条 v103 用例：注入数据 6·摆放默认气 2·渲染 3·换气 3·开关 4；含旧断言修正——接口图例收起断言改按内容判，不再误伤独立气体图例）；HTML 1369.9 KB。
  影响：页面：布局试摆摆气体散布机时画布显示半透明方形环境圈，选中后可换气变色；工具栏新增「环境圈」开关，默认开。
  **v104 更新（环境依赖透明化 · 四色按游戏校准 · 就地选气）**：游戏截图三连反馈全落地：① **四色校准**——游戏实拍稳定=青蓝/湿润=白/酸性=橙黄/息壤=翠绿，v103 按特效资源名猜色把稳定/湿润对反（资源名 scope_white/blue 是内部命名非显示色），ENV_HEX 键改 GenEnv 数字直映射，白圈（湿润）专属高浓度不然浅画布上看不见；② **就地选气**——单击散布机机器正上方浮出四色块按钮即点即换（v103 左栏入口不易发现，撤掉并入画布），支持多台批量；③ **环境依赖透明化**——探针实锤排布器从第一天起就按省料的环境版配方算（息壤粉链=洪炉气液模式、富集气链=提纯机分离芯2→1、灼铜链整组挂环境），但从未告知玩家，没摆散布机这些机器游戏里不工作：产线报告新增「💨 环境依赖」段点名机器/台数/所需气体，配方页 5 条配方标「需XX环境」标签，帮助手册补完全覆盖/一台罩多台/通气6单位每分（官方面板口径，替换v103的「单位未标注」）；recipeEnv 5 条经 build 注入层补进 DB（data 打包时裁掉了 gasEnv）
  验证：692/692 日常档全绿 + HEAVY 全量 706/706（+11 v104 用例：校准色3·就地选4·recipeEnv注入1·报告段2·配方页标签2）；HTML 1376.3 KB。
  影响：页面：散布机圈色与游戏一致、点机器就地换气、产线报告点名环境依赖、配方页标环境要求；排布计算零改动（口径本来就是环境版最优）。
  **v105 更新（v105 修复:点选气条色块误摆新散布机）**：色块浮在画布内,mousedown 冒泡进画布摆放链→清选中→松手 Lput 多摆一座;LonMouseDown 入口对 .lo-gasbar 目标早退即修。事件层补 2 条回归断言,顺带把迷你 DOM 的 closest 修成沿链逐层匹配(旧的一层式 mock 会把 cellEl.closest('.lo-gasbar') 错判成真值,先撞出撤销栈串味再暴露)
  验证：test_html 692/692 日常档 + test_layout_events 75/75 全绿;push_kb 字节校验 2/2；HTML 1376.7 KB。
  影响：点颜色只改气种,不再多摆机器。
  **v106 更新（传送带/管道一格拐弯画不出来：弯头三部件自相矛盾）**：游戏截图对照：弯头格该是『左进下出』的 L 形弯头,试摆器里进色条画上边、弯道弧却从左边绕——rot 单值模型推的『进=出的反向』与真实拓扑(flowIn 由邻居反推)在拐弯处不同轴。修复=进色条与 tooltip 的『进』都改用真实拓扑,与弯道弧同一口径;管道同一分支一并修;功能件(汇/分/桥/阀)多边进出不适用单进边,保持按配置表画
  验证：test_html 700/700(+8 v106 断言)+ test_layout_events 75/75 全绿;沙箱探针 4 场景(同手势L形/分两段/管道/贴机器)弯头格三部件一致；HTML 1377.4 KB。
  影响：拐弯铺出来就是游戏那个观感:进色条、出色条、弯道弧、出口箭头同轴。
  **v107 更新（拐弯第一轴跟手势 + 孤格带子接机器口）**：两张截图反馈：①拉拐弯想要『先上后右』那种,旧版按总位移大小定轴(|dx|>=|dy|先横)画成镜像——改为记录鼠标轨迹,第一个偏移过1格的轴就是第一轴(先往哪拖先铺哪),无轨迹/斜拖回退旧判定;②对角机器夹角的单格带子『还是不行』——flowIn 只查带子邻居,孤格两边都是机器推不出进边,现在机器出料口外格也算拓扑(按介质匹配口),进色条/tooltip/弯道弧同口径;踩坑:LportDir 的 u/d 与色条体系 t/b 两套命名,LOGI_OPP['u'] 是 undefined,已转译
  验证：test_html 706/706(+6)+ test_layout_events 78/78(+3)全绿;探针4场景:轨迹先竖后横/无hist回退/孤格贴口进条在机器侧/带链优先；HTML 1379.2 KB。
  影响：拐弯方向跟手感走;对角机器间单格带子显示正确的进料方向。
  **v108 更新（从机器口（红圈）起手拉带子：自动从口外那一格开始铺）**：游戏截图对照：游戏里是在机器朝外的输出口(红圈)上起手,带子从口外那一格开始铺。旧版起手格若被机器占着,Lfree 判定失败压根进不了连铺——『从红圈起手』根本做不到。放宽:起手格压机器、但口外那格空着→起点自动移到口外一格。LsnapStart 改成纯探测(只返回坐标不改拖拽态),避免探测污染状态;整段手势仍只压一次撤销栈
  验证：test_html 710/710(+4)+ test_layout_events 81/81(+3)全绿;探针3场景:口格起手吸到口外/口外被占不起手/空地起手回归；HTML 1380.6 KB。
  影响：从机器输出口直接拉带子,手感与游戏一致,不再需要先点口外那格。
  **v109 更新（机器口点击分流 + 协议核心出货选择）**：① 手拿物流件按在机器口格上不再直接拖机器：原地松手=从口外拉线、拖过 4px 才移机器（新增 portpend 待决态）。② 协议核心 6 个出料口在格内画朝外的可点箭头，点开列出该核心所属域的可出货物品（FactoryItemTable.deliverItemTypeList，281 件；四号谷地 70 / 武陵 211），选中后箭头变绿并标出物品名，再点同类取消。
  验证：日常档 732/732（+22 条 v109 断言）+ 事件层 103/103（+13 条）；HEAVY=1 全量 746/746；真机 6 箭头/70 候选/选中回显全通；HTML 1419.3 KB。
  影响：布局页新增「出货物品」选择维度；出货记录独立于摆放快照（撤销不影响）；机器口交互与旧习惯（拖动移机器）兼容。
  **v110–v117 更新（工具链与发布链路：页面功能无变更）**：这八版全部落在「怎么构建、怎么发布」上，页面功能未动 ——
  v110 **版权隔离与离线构建**：raw 依赖烘焙化（`tools/bake_raw.py` → `data/raw_baked.json`），`build_html.py` 优先读烘焙文件、
  缺了才回落 `raw/` —— 没有 raw 的机器（云端环境、新克隆副本）也能构建出**字节完全一致**的产物（详见「数据分层与版权」）；
  同期建起 Git 仓库与 `tools/gh_deploy.py`（Git Data API 部署通道，主站限流环境下的唯一可用路径）。
  v111–v112 **代码可读性**：新增《排布器算法地图》（算法入口索引）+ 核心函数注释与分节路标；同步做代码质量整改。
  v113 **开源落地页与隐私清理**：仓库根新增 `index.html`（11 KB 纯静态单文件、零依赖、离线可开，首屏指路在线版）+ README
  「在线体验」入口；对外文档与数据字符串里的私人称呼一律改为中性措辞（代码注释按约定保留）。
  v114–v116 **上线安全加固**：修掉内部目录文件被误上传的问题 —— `push_kb.py` 在上传列表**确定之后**再加一道硬闸，
  内部目录（任一级以 `.` / `_` 开头）与排除目录一律拒绝，手动 `--files` 指定也拦（只靠扫描期软过滤不够：本地文件还在，
  任何一条上传路径都可能把它重新推上去）；配套 `tools/purge_online.py`（线上覆盖占位，`--dry-run` 预览）与
  `tools/test_push_kb_guard.py`（防线回归 14 用例）。同期 README 补分享链接口径：只写 `space/d/` 形式，
  含版本号的静态直链禁止写进任何长期留存处。
  v117 **开源可用性闭环**：新增 `tools/fetch_raw.py` —— 一条命令从数据域拉齐 `raw/`（内嵌 66 张表清单、并发 6 + 重试 3 +
  JSON 校验 + 下载日志），默认版本取自 `data/meta.json`。仓库不含 raw，clone 的人自己能补齐（详见「怎么更新烘焙」）。
  验证：产物 sha256 在「有 raw / 无 raw」两种环境下一致；防线回归 14 用例（离线 10 通过，另 4 例需线上凭据实测）；
  `gh_deploy` 版权检查「无 raw/ 文件」通过；fetch_raw 抽样 4 表与既有 raw/ 的 sha256 逐一相符。
  影响：clone 下来就能构建，不必有 raw；对外文本去私人化；内部文件不再有被推上线的路径；换游戏版本时 raw 可一键补齐。
  **v118 更新（推送工具自愈：防线不再堵死发布通道 + 终验不再误报）**：修掉两个自己挖的坑 —— ① 上传硬闸原来是「发现内部文件就整批中止」，而那些文件是线上残留占位、线上产物树里永远存在 → auto 模式从此每次必挂，发布通道被防线自己堵死；现改为「剔除 + 明确告警」（剔除点仍在 push 列表确定后的最后一步，安全性等价），只有 `--files` 显式点名内部路径时才中止。
  ② 终验对 HTML 必然误报 FAIL：平台除注入 `data-page-node-id` / `inject.js` 外，还会**重排属性顺序**（本地 `name` 在前、线上 `content` 在前）、并给内联 CSS 的分号后补一个空格，现有剥离逻辑覆盖不到；补上「开始标签属性按名排序」+「`style` 值分隔符两侧空白归一」+「删注入脚本时连带吃掉换行」后归零。
  验证：防线回归 14 → **22 用例**（离线 16 通过 / 带凭据 22 全过）；v118 真实 auto 推送终验 **3/3 通过**（此前必出 FAIL）；test_html 732/fail=0、事件层 103/fail=0，产物字节一致。
  影响：auto 模式恢复可用；推送末尾不再出现假的 FAIL；内部文件仍然一条都上不了线。
**v121 更新（访客分享链接修复：短链复活 + 推送链补 publish）**：排查「分享出去的链接要求登录」—— 实锤 `workbuddy.cn/space/d/` 是登录态入口（平台设计），访客免登录分享的正解是 `workbuddy.link/p/` 发布短链（实测无凭据可开、带社交分享卡、URL 永不变）；但它停在 v116 —— 根因是 `push_kb.py` 从建链以来只 commit 从不 publish，访客发布视图落后四版。修两处：① 仓库根 `index.html` 回归托管页当跳板（v120 曾把线上那份下架成占位页）：在托管域下自动跳转成品页，GitHub / 本地打开仍是介绍页，`SCAN_SKIP_FILES` 排除名单随之清空；② `push_kb.py` commit 后自动 publish，终验新增「publishVersion 必须追上本次 commit」核对。
验证：守卫回归 18 离线通过（index.html 三条断言随策略反转翻面）；test_html 732/fail=0、事件层 103/fail=0，产物 1425.7 KB 字节一致；发布后无登录态实测短链直达成品页。
影响：朋友点链接就能用，不再被登录墙挡住；以后推送一条命令到底，不会再出现访客看着旧版的暗坑。
**v122 更新（布局页修复：旋转机器后接口朝向跟转，进出货口不再错位）**：博士实测截图「旋转个方向进出货口就不齐了」—— 根因是接口朝向靠「口格贴在占地方框哪条边」来猜，角上的口两条边都压、代码固定取 z 边；rot=0 时配置表口径恰好成立（进料口数据就在下边）所以全对，机器一旋转，口的位置被占地旋转公式转到了新边，朝向判定却对转完的坐标重新贴边猜 → 全错。「口外那一格」是端点吸附、从口拉线、接口已接统计、自动布线的共同依据，一处错处处歪。修复：新增 `LportDirRot`（rot=0 按原口径判一次基准朝向，之后按 d→l→u→r 步进转 n 次，与占地旋转同一套几何），六处调用点全部切换；顺带修掉端点吸附与口上点击两处误传「旋转后宽深」的问题（非正方形建筑接口会错位）。配置表 `facing` 字段考察过：同座精炼炉左墙进口与右墙出口 facing 同值，是模型局部参数不是朝外方向，不采用。
验证：新增 13 条回归断言（rot=0 回归不变 + 四向朝向 + 角口实锤 + 端到端「旋转 90° 后左侧铺带接口已接 ≥1」「出料口起手吸附到右侧口外」），test_html **745/745**、事件层 103/103 全绿；产物 1427.8 KB。
影响：机器随便转，带子照着口铺就齐；自动布线在旋转过的机器之间不再歪。
**v130 更新（工具链提效三件套（本条由 release_note.py 首战自动写入））**：v129 把 README 拆成 5 份文档后，release_note.py 因锚点迁移静默罢工——改为目标探测制（锚点在哪个候选文件就写哪个）+ 版本号显式传入（--version，扫描推断仅兜底——首战曾把 v130 标成 v129，删掉重插的教训：工具罢工期间发布的版本不在文档里，最大+1 必然断链）；新建 check_docs.py 文档健康门禁四项（相对链接有效 / changelog 锚点恰一处 / README·docs 禁登录态链接只准 workbuddy.link/p/ 短链 / 基线行可解析），FAIL 非零退出；重写 release.py 发布一条龙（--gh 自动暂存推 GitHub / --docs-only 纯文档跳构建 / --commit 本地存档），测试步骤自动抓 RESULT 行与 docs 基线行对账——文档数字过时直接发不出去。
验证：check_docs 四项全绿；release.py 本地全链 35.4s 七步全绿（778/778 skip=14 · 106/106 · 基线对账一致）；HTML 1437.3 KB。
影响：发布从记多套命令收敛为一条；文档面健康检查常态化；README 警示块瘦身、docs/维护与更新.md 新增分享链接口径节。
**v131 更新（建筑分类对齐游戏官方分组）**：七类自造名换成游戏内「工业设备」面板官方分组名（溯源 FactoryQuickBarTypeTable）：资源采集→资源开采、基础加工→基础生产、组件加工→合成制造、物流辅助→功能设备、仓储物流→仓储存取、防御设施→战斗辅助；建筑页列表与试摆左栏改按官方组序排列（存取线基段与源桩从此同组相邻），物流件归「物流」组；默认清单与拉黑口径不变
验证：页面回归 778/778（skip 14）· 事件回归 106/106 · check_docs 8 md 全过；ake --分类 电力 5 条 / 战斗辅助 18 条与数据核对一致；HTML 1439.9 KB。
影响：页面与 CLI 的分类名、分组排序变化（用户可见）；categories.json 结构更新（source/names/order）；数据手册/README/ake 帮助同步改名。
**v132 更新（分组数据跟着 AKEDatabase 全自动同步）**：建筑分组名/组序从 build.py 手填快照改为构建时动态读 raw/FactoryQuickBarTypeTable（AKEDatabase 数据域，fetch_raw.py 清单已登记）：游戏版本更新后重拉数据重跑构建即自动跟上，上游加新组自动获得灰底+首字字标；快照降级为回落+对账基准；建筑页分类下拉也按官方组序排序（v131 漏项顺手补）；docs 补分组表同步 SOP
验证：动态生成三表与 v131 静态值逐键一致；fetch_raw 真拉 OK/FAIL=0 后重跑 build 对账干净；release.py 全链 36.6s 六步全绿（778/778 skip=14 · 106/106 · 基线对账一致）；HTML 1440.2 KB。
影响：构建链分组数据源从快照变动态（mode:dynamic）；产物仅分类下拉排序与注释微变。
**v133 更新（扩容反应池同池并行（游戏机制核实 + 台数修正））**：官方文案+实机+配置表三重核实「一栋 8 缓存格、同一时间可跑多条不同配方，同一条配方不叠加提速」；实现：同组 ≥2 条不同反应 → 整组升扩容池、台数取组内 max（每栋对每条反应各贡献 1 份产能），buffers 物品并集 >8 格时贪心分组；单配方链保持基础池（扩容池更贵更大、无并行收益）；报告新增「扩容反应池同池并行」行
验证：781/781（skip 14）· 106/106 · release.py 全链 38.0s 全绿；壤晶链 18→16 栋、赤铜块链保持基础池；孪生变体改用原料/产物签名匹配（两池 group 名不同）；HTML 1497.7 KB。
影响：反应池类产线的机器数与用电更贴近游戏实际；machine_recipes 补 buffers 字段（317/317）；新增 3 条回归锁。
**v134 更新（沙盘反应池面板（缓存格 + 输出产物槽，对齐游戏交互））**：布局试摆里选中反应池/扩容反应池 → 左栏出现专用面板：8 个缓存格（按已选反应的进料+出料去重推演，超 8 格标红警告）+ 输出产物槽（扩容池 3 槽/基础池 2 槽，每槽一条反应可选，右侧实时显示该反应产出速率）；沙盘侧池子改用 o.rl 反应数组（兼容旧 o.r 单值），格子上多条反应标「同池 N 条反应」；缓存格 = 静态推演（不代表游戏内实时时序）
验证：784/784（skip 14，含 3 条新回归锁：8 格推演与实机逐项吻合/池子上限/读取兼容）· 106/106；浏览器真人驱动验证：摆扩容池→设 3 条反应→面板渲染 8 格 + 3 槽 + 产出速率；HTML 1503.1 KB。
影响：沙盘交互对齐游戏（博士 2026-09-24 需求）；暂不改自动排布链路（沙盘手动选择优先）。
**v135 更新（点机器就地选（框架 + 反应池面板搬上画布））**：单击沙盘上的机器 → 机器上方弹出该机器的选择浮层（游戏同款交互，沿用协议核心出货浮层的定位/样式语言）；浮层内容按机器类型分派 RmacPanelOf —— 反应池/扩容池 = 缓存格（8 格物品卡）+ 输出产物槽，其他有配方的机器 = 配方选择 + 产能读数；新增基建只需在分派里加一个分支（博士要的「逐步完善到其他基建」挂点）；设置改为单机生效（浮层语义），左栏池子面板撤掉避免双入口；界面上只呈现一个「反应池」条目（数据=扩容反应池：占地 6×5 / 100 电 / 8 缓存格 / 3 条并行；基础反应池 mix_pool_1 移出沙盘清单——配置表 range 与渲染模板名 06x05 双证扩容池确为 6×5，博士质疑的「都是 5×5」已核对留档）；⭐同轮按实机修正反应池三处：占地 6×5→**5×5**、端口 12 个→**8 个**（每边各 2：带 2 进 2 出 / 管 2 进 2 出，配置表按 6 格宽写的那套含越界 x=5）、名字只留「反应池」——配方组端口绑定同步改为基础池索引（否则旧绑定 [4,5] 越界，被「接口序号越界」护栏抓住）
验证：786/786（skip 14，含 2 条新回归锁：分派/槽位数）+ 106/106 · release.py 全链 36.3s 全绿；浏览器实测点击链路（真派发 mousedown→mouseup → macPop 打开、浮层 7/8 格 + 3 槽 + 重复选择去重）；HTML 1510.6 KB。
影响：沙盘交互对齐游戏：点机器就地选（左栏那份池子面板已撤）；框架可扩展到其他基建。
**v136 更新（循环诊断补警告 + 台数口径透明化（对标调研 D+A））**：D：绕不开的回收路线 / 链深上限以前只写在节点 note 上、报告不出声（静默降级）→ 现在明确进警告列表，并给处理办法（开局给一次料让它自持，或打开「闭环自持」算启动清单）；A：报告新增「台数口径」行 —— 理论分数台数（各台需求÷单台产能求和）→ 实际整台 → 取整多出几台 / 主要来自哪几台，并写明「机器只能整台摆」的口径；B：同物品多配方改按「单位原料成本（每轮成本÷产出数）→ 每轮耗电（单台耗电÷轮数）→ 单台产出」三层**分层比较**（零加权魔法数字）——紫晶质瓶从拆解机(20电)改选塑形机(10电)省一半电，清水类仍原料优先
验证：788/788（skip 14，含 2 条新回归锁：external 进警告 / 报告含台数口径行）+ 106/106 · release.py 全链 36.9s 全绿；HTML 1515.1 KB。
影响：报告不再静默降级、能解释「为什么凭空多一台」；对标调研剩 B（配方选择升级）未做。
**v137 更新（手动连铺十字相交自动建桥（对齐游戏））**：博士：「游戏里两条传送带相交后会自动建物流桥，试摆里没有」——核实发现自动连线早有桥逻辑（RwPath BRIDGE 代价 + log_connector/log_pipe_connector），缺的是**手动连铺**：连铺遇占用一律跳过，交叉处只有原线。改为「同类介质的 1×1 普通带/管占着 → 在交叉格叠一座桥」（原线保留），其他占用（建筑/分汇流器/已有桥/异类介质）仍跳过；提示语「已在相交处叠了一座物流桥」
验证：791/791（skip 14，含 2 条新回归锁：传送带十字→log_connector / 管道十字→log_pipe_connector）+ 106/106 · release.py 全链 37.0s 六步全绿；浏览器实测两个十字场景：交叉格 = 原线 + 桥 ✓；HTML 1517.3 KB。
影响：手铺物流线对齐游戏行为；修的过程中被既有「连铺跳过建筑」两条断言抓出占用判定漏建筑（已修）。
**v138 更新（准入口：只能叠在带/管上（顺流向）+ 就地面板（准入物品/限速））**：博士：「准入口只可以放在传送带和管道上」「可以选择准入物品，像反应池那样在画布中选」「还可以限速」+ 澄清「转角格不能放是因为没沿着物流方向建造」。P1 放置校验：准入口（物品准入口/管道准入口）必须叠在同类介质的带/管上（空地直接拒绝），且要顺物流方向——转角格（同类邻居呈 L 形）拒绝；放上去朝向沿用原格流向。P2 就地面板（走 RmacPanelOf 分派）：限速档位 6/分一档（带≤30、管≤60，社区核实）+ 准入物品多选（不选=不限）。**P3（吞吐体检按限速判堵）本轮未做**——需给自动连线存路径数据，改动面在 RwRoute 核心，另开一轮验证
验证：794/794（skip 14，含 3 条新回归锁：空地拒绝/转角拒绝/线上成功+面板+写入生效）+ 106/106 · release.py 全链 35.6s 六步全绿；浏览器实测四种场景全通；HTML 1522.5 KB。
影响：沙盘放置规则对齐游戏；准入口可配（限速/准入物品）；限速对瓶颈判定的影响（P3）待做。
**v139 更新（准入口合规标红（孤立 / 拐角，渲染时判定））**：博士：「不是说只能放在传送带上吗，怎么没标红」+「拐角也是怎么还放得下」——放置校验只拦新放，已有的不合规件（旧画布/方案载入/拖拽移动来的）无人管。新增渲染时校验：准入口四邻若无同类带/管衔接（空放）或呈 L 形（拐角，没有直的物流方向）→ 标红描边 + 右上角标，tooltip 给原因与建议；真直线段（对开的一对邻居）放行
验证：795/795（skip 14，+1 回归锁：孤立/拐角/直线三态）+ 106/106 · release.py 全链 35.3s 六步全绿；浏览器实测 isolated=true / corner=true / straight=false；HTML 1524.4 KB。
影响：不合规准入口一眼可见（对齐游戏红字警告）；覆盖所有来源而非只拦新放。P3（限速联动吞吐体检）仍未做。
**v140 更新（准入口三修：朝向语义 / 拐角判定 / 标红可见性）**：博士实测三报：①竖着放准入口方向变横向 ②放在拐弯处会把拐弯变直线 ③单独放外面不标红。根因：**准入口 rot 与传送带 rot 基准差 90°**（传送带 rot0=流向右 / 准入口 rot0=下进上出）——换件时直接沿用原格 rot 就转成了横向（加 LtwinRot 映射修复）；拐弯变直线的根因是替换掉了拐弯格导致拓扑断裂（拐角格现在被拒：进向≠出向）；标红看不见是因为用了橙红、与准入口本身的橙色描边撞车（换纯红粗描边 + 左上角「!」角标）。另把合规判定收成唯一出处 LvalveBad（放置校验 + 渲染标红共用），并补「有上游没下游 = 断头（会把线截断）」的拒绝
验证：795/795（skip 14）+ 106/106 · release.py 全链 36.0s 六步全绿；浏览器实测：竖直带(rot90/流向下)→准入口 rot180 进上出下 ✓；孤立准入口 vbad 计数=1 ✓；HTML 1525.6 KB。
影响：准入口放置与显示全面对齐游戏；判定唯一出处，后续维护不再两处打架。
**v141 更新（准入口三处误判修复（方向语义 / 移动重算 / 弯头拓扑））**：博士实测三报，全部定位到根因并修复：①**方向语义**——LvalveBad 读方向时对阀门也用了传送带语义（阀门 rot 差 90°），竖直段被读成横向 → 误判拐角 → 到处标红（连「竖直带中间放准入口」都红）；改为读前先 rot-90 转回传送带语义。②**移动后不重算朝向**——拖到别的线路上 rot 不更新 → 一直标红；新增 LvalveResync（移动分支调用），按新位置线段流向重设 rot。③**弯头被拉直**——弯头的「上游指向我」是从流向表 flowNext 反推的，而该表只认带/管，准入口成了空气 → 放在转折格旁边的直段格时，转折格反推不到上游、被画成直线；改为把箱阀/管阀也纳入流向表
验证：795/795（skip 14）+ 106/106 · release.py 全链 36.6s 六步全绿；浏览器实测：竖直带中间 vbad=0 ✓ / 水平带中间 vbad=0 ✓ / 孤立 vbad=1 ✓ / 移动后 rot 自动重算 vbad=0 ✓ / 弯头前后 SVG 完全一致（bendSame=true）✓；HTML 1527.5 KB。
影响：准入口放置判定与显示全部对齐游戏；不再误报也不再破坏已有走线。
<!-- changelog:next —— 版本条目固定插在这行注释之前（tools/release_note.py 自动插入；条目用三行短格式：结论一句 + 验证数字 + 影响） -->
- **报告里给什么**：需要多少/分 · 单位物品价值 · 每批间隔；**填上「传输总值」才会算出**每批能带多少、供货速率多少
  （不填就不给数字 —— 配置表里没有档位表，瞎编就是假数据）。**供货速率低于需求会直接标红提示喂不饱**。
- **配套修正**：收货的料在「原料野外上限」里标成**本地不采、不计野外上限**，在「原料侧闭环」里也**不再给它配矿机**
  —— 两个地方原来还在按本地矿脉算，跟"这批货是传输来的"自相矛盾（同一份报告会同时写着收货和「要 1 台矿机」）。

> ⚠️ 别忘了上面的硬约束：**集成工业同时只在两个地区运行**、每批**只能传一种物品**、供货有**速率上限**。
> 所以 ⑥ 的真问题里「**多目标共享中间产物**」已在 v76 落地（见下节），只剩「**跨基地选点**」——
> 传输本身只是给货运开了个配额窗口，它把"两地的距离"变成"一条线路的配额"，但没有把"同时开 8 片基地"变成可能。
- 验收（v77 预填 + v79 单选 + v80 网格 + v81 全物品可选 + v82 全量清单）：`test_html.js` **633**（v77 +2：空值预填 1500 / 手填不覆盖；
  v79 +4：双可传链只收一种 / 默认 = 需求最大者 / 换选后另一种回退本地 / 网格只在多候选带标题；
  v80 +2：双候选网格两张卡恰一张选中带「每批能带」/ 单候选单卡无标题 + 开关即重算关掉即消失；
  v81 +3：半成品换选砍上游 / 需求口径稳定 / 面板即选；
  v82 +2：全量折叠网格+搜索接线 / 链外选中提示+换目标刷新），
  `test_layout_events.js` 72。

### 多目标共享中间产物（路线图 ⑥-2 · v76 —— 只建一套，再分流）

工具栏「＋ 目标」可给产线**再加最多 3 个目标**（各填一个 速率/分）。所有目标进**同一张需求 DAG**：
共用中间料只建一套产线，出料再分流给各目标 —— 不用为每个目标把同一条上游链各摆一遍。

- **合并重算**：共享节点按「各目标需求之和」重新上机器台数（⌈需求 / 单机速率⌉，整台实出），
  深度取最深消费者 +1；两轮推演（原始 / 带进货）口径不变；「进货不落地」等原有开关照常生效。
- **不保证省机器**：整台进位不重合时，合并台数 = 分开建之和；省的是**同一套上游只摆一遍**
  （一条矿线、一套水线），进位恰好重合时还能更少。测试锁的口径是「合并 ≤ 分开之和」。
- **报告**：头部目标列表（「 ＋ 」连接）；新增**共用中间料**段（几台 · 合并需求 · 整台实出 → 分给谁多少/分）；
  原料取各目标并集；管线明细里共用节点按消费者各一行。
- **实测**：赤铜耐压罐@10 ＋ 赤铜瓶@10 → 共用赤铜块 8 台（合并需求 40/分）、共用清水 8 台。
  ⚠️ 这种双目标长链默认 50×50 画布放不下，换 80×80。
- 验收：`test_html.js` 620（+20，含 ⑥-2×⑥-1 组合：收货吃合并需求 40/分、共用段与收货段同屏），`test_layout_events.js` 72。

### 跨基地选点（路线图 ⑥-3 · v83 —— 分配建议器，⑥ 收官）

问题：N 个目标（主 + ＋目标，≤4）放哪个地区的基地「更省」。**v1 只出建议不摆画布**（2026-09-22 定
建议器方案，否掉「两地同时摆」多画布）—— 报告尾部常驻一段「哪个成品放哪片基地更省」，布局面板「选点建议」开关同款渲染。

- **方向下拉（两地对称互传）**：收货方向从写死「四号谷地 → 武陵」改为**从（）到（）双下拉**，
  地区清单动态读 `bases.json domains` —— 未来 domain_3 开放**零改动接入**（测试锁：push 假地区后自动出现）。
  **换向**：两地区现状下单端下拉选另一端 = 交换两端（中间态必然同端，不这么解释「武陵 → 谷地」就永远配不出来）；
  选自己 = 无操作提示、不清收货选择。**切基地**时「到」自动对齐基地所在地区（货要进产线所在地区的仓库才有用），
  「从」被顶成同区时自动让位；有产线时改方向即按新方向重算。
- **数据依据（全在库，不造数）**：矿脉按地区（源矿 560/540 · 紫晶 240/0 · 蓝铁 1080/120 · 赤铜 0/510）；
  机器地区限定（天有洪炉等 9 座武陵限定 → 息壤链谷地**硬否决**）；`RwMade(region)` 按地区过滤机器配方
  （息壤粉末 从谷地 1 条转质 / 从武陵 3 条；收货候选 全部 162 / 从谷地 161 / 从武陵 162）。
- **成本口径（2^N 组合穷举，逐项可解释）**：硬否决 > 同方向多种收货物冲突（每方向每批只传一种，×1000）>
  口径①喂不饱（×300）> 分两地重复建共享料（×10）> 矿缺口量（×1）。
- **口径①（地区合计）**：同区多目标要同一种收货物 → 按**地区合计需求**对每批可到货量（传输总值÷单价，
  满级 1500 ÷ 批次间隔 1h）反推。实测：罐@30 全放谷地 → 赤铜矿缺 60/分 · 供货约 25/分 → **喂不饱**；
  罐@30 + 瓶@30 同区 → 合计 120/分（不是各基地各算各的）。
- **口径②（取货段）**：地区仓库取货口「仓库取货口」3×1×3 → 各基地：单边路数 =（边长−1）÷3 向下取整
  （谷地 23/13 实测、武陵 26/16 推算 —— 页面按来源分开标注）；**吞吐配置表里没有单列数据 → 明说不编数字**。
- **自筹料点名（不静默漏报）**：惰性壤晶废液这类「不能跨地区传输 + 产线未建模（聚合池/拆解自筹）」的原料，
  对比行里点名给量（罐链 80/分 + 惰气 10/分），不计缺口也不进成本 —— 算不了就明说算不了。
- **实测**：罐@10 → 武陵（cost 0，赤铜矿本地够）；罐@10 + 紫晶纤维@10 → **分居两地**
  （罐→武陵吃 510 的赤铜矿、纤维→谷地吃 240 的紫晶，两边都无需收货）。
- 验收：`test_html.js` **667**（633 → +34：方向下拉 2 / 换向 4 / 切基地对齐 2 / RwMade 过滤 3 /
  矿 cap 与取货口 4 / RxlAnalyze 4 / 硬否决 2 / RxlBest 4 / 口径① 4 / 渲染 4 / 门禁 smoke +1），
  `test_layout_events.js` 72。
