# 排布器算法地图

> 面向**第一次读这份代码的人**。
> 排布器有 117 个函数，但真正需要理解的只有 **6 个核心**。这份文档告诉你：它们各解决什么问题、按什么顺序读、互相怎么调用。
>
> 全部代码在 `tools/build_html.py`（单文件，行号对应当前版本）。

---

## 一、先建立全局图景

排布器做一件事：**你给一个目标物品和速率，它算出一条完整产线并摆到画布上。**

```
你输入：目标物品（如「息壤」）+ 速率（如 30/分）
        ↓
  ① 展开配方树  —— 这件东西怎么做？要哪些料？料的料呢？
        ↓
  ② 分层布局    —— 按「上游在下、下游在上」一层层摆机器
        ↓
  ③ 布线        —— 把机器之间的传送带/管道连起来
        ↓
  ④ 打分        —— 摆得好不好？不行就换个参数再来
        ↓
你得到：画布上的完整布局 + 一份体检报告
```

**①②③④ 各自对应一个核心函数，外面再套一层「参数搜索」反复试。**

---

## 二、调用链（实测，非推测）

```
LawRun ─────────────────────────────────── 总控 + 参数搜索（L3661，198 行）
  │
  ├─ Rexplode ──────────────────────────── 配方树展开（L2870，159 行）
  │    ├─ RwCost ────────────────────────── 子树代价估算（L2832，5 行）
  │    └─ RexplodeFinal ─────────────────── 收尾：幽灵边/告警（L3035，58 行）
  │
  ├─ LawPlan ───────────────────────────── 分层布局（L3127，124 行）
  │
  ├─ RwRoute ───────────────────────────── 布线总控（L3267，293 行）
  │    ├─ RwFindSplit ───────────────────── 找分流器位置（L3561）
  │    ├─ RwFindMerge ───────────────────── 找汇流器位置（L3579）
  │    └─ RwPath ────────────────────────── ★ 最短路（L3602，57 行）
  │
  └─ pickScore ─────────────────────────── 单次布局打分（L3719，142 行）
```

### 📖 推荐的阅读顺序

**`LawRun` → `Rexplode` → `LawPlan` → `RwRoute` → `RwPath`**

先看总控怎么调度（知道全局节奏），再看每个环节怎么做。
**反过来读会迷路** —— 直接读 `RwPath` 的 57 行，你不知道它在给谁算路。

---

## 三、六个核心逐个说

### ① `LawRun` — 总控 + 参数搜索（L3661）

**它不只是"跑一遍"，它是个优化器。**

做的三件事：
1. **枚举参数组合**：间距 `gapX` / 是否列对齐 `align` / 通道宽度 `corr` / 布局模式 `mode`，每组跑一遍完整流程
2. **相邻交换爬山**：对每层的机器做相邻两两交换，最多试 24 次，分数更高就留下
3. **通道宽度梯度上升**：`corr` 不够就 `+2` 重试

每组都走 `Rexplode → LawPlan → RwRoute → pickScore`，最后留分最高的。会把"试了 N 组参数（含相邻交换 M 次）"回报给界面。

**为什么难**：它是**搜索**，不是流水线。三层嵌套（枚举 × 交换 × 宽度），但每层都是独立的，有回归测试护着。

---

### ② `Rexplode` — 配方树展开（L2870）

**递归**：这件物品怎么造 → 要哪些料 → 那些料怎么造 → …直到原料。

两个关键设计：

**a. `pick()` 的三级降级策略**（L2897）——这段是精华：
```
① 首选「不吃自己 且 不是回收」的生产配方
② 没有纯生产配方 → 只剩回收路线
     ├ 默认：当外部输入（别往里钻，防止无限递归）
     └ 开了「闭环自持」：钻进去，让载体变成启动料
③ 最后才退回载体/拆分路线
```

**b. memo 去重 + 多目标共享**：同一件中间料只建**一套**机器。多目标模式下（同时做两件东西），共享的中间料合需求、只建一套，各消费者挂"幽灵边"回指。

**⚠️ 改这个函数要注意**：注释里明确写了「不传 seeds 就是单目标老路径，一行都不多跑（601 项回归逐字节依赖它）」。**加参数可以，改老路径会被回归抓到。**

---

### ③ `LawPlan` — 分层布局（L3127）

**按层摆机器**：`depth` 从小到大（从成品到原料），一层一行。

核心技巧是**列对齐**：深层的机器按「消费它产出的下游机器的 x 中心均值」排序 —— 喂同一下游的机器聚到那台机器正下方，**走线从"绕"变"直上直下"**。

还支持：
- 局部锁定（`opts.fixed`）：已固定的机器当障碍物，新机器避让
- 层内主动换行：上游比下游宽时（12 台块机 vs 6 台罐机），按下游分组换行
- 折行行距降级（v99）：纵向爆就 `-2` 递归降级，下限 3

**⚠️ 同上的铁律**：`fixed` 为空、`rowGap` 不传时，**必须与老行为逐格一致**（520 条回归依赖）。

---

### ④ `RwRoute` — 布线总控（L3267，293 行，**全项目最长**）

把布局好的机器连成产线。处理：多点对多点的连接、传送带/管道区分、分流与汇流。

它主要干三件事：调 `RwFindSplit` 找分流点、调 `RwFindMerge` 找汇流点、调 `RwPath` 算具体路径。

**为什么 293 行**：分支极多（8 种介质 × 多源多汇 × 已有线的避让），但没有深层嵌套。

---

### ⑤ `RwPath` — 最短路（L3602）★ **全项目最硬的一段**

**手写的 Dijkstra 最短路径**，不是调库。

实现要点：
- **手写二叉堆**：`hpush` / `hpop`（L3617-3626），没依赖任何库
- **四维状态**：`(x, y, 方向, 是否在桥上)`
  - 为什么带方向？因为**转向有代价**（`TURN=2`），路线要尽量直
  - 为什么带桥？踩桥也有代价（`BRIDGE=4`），且桥上只能直行
- **代价模型**：直行 `1` / 转向 `+2` / 踩桥 `+4`
- **稳定排序技巧**：`+0.0001`（L3647）避免同代价时的抖动

**"桥接"是什么**：已经铺好的线本来是墙，但允许**正交直穿** —— 你走向和被穿的线正交、且穿过去不转弯时，可以铺一座"物流桥"过一格（代价 +4）。比绕远路便宜时自动启用。同向重叠依然禁止（会互相顶）。

**这段代码的水平**：拿到任何工业级仓库里都算合格。有算法基础的人 15 分钟能读懂；**没有基础的人需要先了解 Dijkstra**。

---

### ⑥ `pickScore` / `Rscore` — 打分（L3719 / L4757）

一次布局好不好，5 项加权：紧凑度 / 机器数 / 线长 / 核心连通 / 浪费空间（`RW_W` 定义权重）。

`pickScore` 是给搜索用的快评，`Rscore` 是给用户看的详评。

---

## 四、除核心外的两层

| 层 | 数量 | 特点 |
|---|---|---|
| **短函数**（<10 行） | **67 个** | 查表、取值、一行转发。**任何人都看得懂** |
| **长函数**（30-120 行） | 15 个 | 长但**不难**：鼠标事件处理、报告渲染、HTML 拼装 |

**短函数长这样**：
```js
function RbyId(id){ return (DB.machine_recipes||[]).filter(r=>r.id===id)[0]||null; }
function Rrounds(r){ return (r&&r.seconds)?(60/r.seconds):0; }
function RwLines(pm,isPipe){ return (!pm||pm<=0)?0:Math.ceil(pm/(isPipe?RW_PIPE:RW_BELT)); }
```

**结论**：117 个函数里，**只有 6 个需要真正动脑**，其余是"看一眼就懂"或"长但直白"。

---

## 五、改代码前的三条铁律

这份代码有两个反复出现的约束，**改任何核心函数前必看**：

### 1. 老路径必须逐字节一致
多处注释写着「不传 X 时一行都不多跑，N 条回归逐字节依赖它」。**加新参数可以，改老行为会被 732 项回归立刻抓到。**

### 2. 参数命名陷阱
`Rexplode` 里有一条：
> ⚠️ 参数名用 `shipInList`，别用 `shipIn` —— 那是 `Linit()` 里的**布尔开关**，同名会炸。

### 3. 改完必跑
```bash
python tools/build_html.py           # 重建
node tools/test_html.js              # 732 项回归（必须 fail=0）
node tools/test_layout_events.js     # 103 项事件层
```

---

## 五之二、哪些「该拆」其实不该拆（2026-09-23 定案）

读代码时容易产生「这个函数太长，该拆」的冲动。**以下三处经实测判定：不拆，理由如下。**

| 对象 | 行数 | 表面症状 | 为什么不拆 |
|---|---|---|---|
| `build.py::main()` | 1111 | 超长函数 | **伪神函数**：21 个分节标记、每 3 行一条顶层语句、线性数据装配。拆了反而打断「一眼看完整个构建流程」的好处 |
| `RwRoute` | 294 | 超长函数 | **已有 27 条分节注释 + 两阶段 `====` 分界**，结构清晰。属于「长但不乱」 |
| `LawRun` 的爬山段 | ~120 | 想抽成 `LawHillClimb` | **它不是独立算法**，是「反复调 `LawPlan`+`RwRoute`+`pickScore`」的搜索驱动代码。抽出去要传 `res`/`size`/`depLines`/`best`/`tried`/`pickScore` **六个上下文**，签名比函数体还长；且三段补搜（宽间距 / 交换 / 通道爬升）**共享 `best`/`tried` 且顺序敏感**，拆开易出新 bug。→ **改插 7 段 `[1]~[8]` 路标代替** |

**判断口径**：拆函数的收益是「少滚动」，成本是「多一层跳转 + 参数传递」。**当函数是线性流程或已有分节时，加路标比拆函数更划算。**

---

## 六、术语快查

| 前缀 | 含义 | 例子 |
|---|---|---|
| `L*` | 界面状态 / 鼠标交互 | `Lput` / `LonMouseDown` / `Linit` |
| `R*` | 配方 / 数据查询 | `RbyId` / `Rrounds` / `Rcarriers` |
| `Rw*` | 产线逻辑（Recipe → wire） | `RwRoute` / `RwPath` / `RwCost` |
| `Law*` | 布局求解（Layout） | `LawPlan` / `LawRun` |
| `Rxl*` | 跨基地选点 | `RxlAnalyze` / `RxlBest` |

**读代码的窍门**：看前缀就知道这个函数属于哪一层，不用逐个点进去。
