# IndustrialPlanner 源码规则提炼

> 来源：github.com/hsyhhssyy/IndustrialPlanner（MIT，v1.5.1 @ 2026-09-20，数据源 AKEData 与本库同上游）。
> 它 = 手动画布 + 物理仿真；本库排布器 = 自动规划。本文只记「能抄的规则」，不是使用教程。
> 提炼日期：2026-09-21。关键源文件：`src/editor/actions/logistics-action.ts`、`src/editor/logistics/logistics-utils.ts`、`src/simulation/runtime/*`、`src/simulation/contracts/tick-rate.ts`。

## 一、编辑器物流连接规则（供 RwRoute / 单基地优化参照）

物流是**逐格实体**：每格一个弯道/直道/分流/汇流定义；画新线通过 `cellOverrides` 精确改写受影响格，其余格不动。三种自动设备 `AutoDeviceKind = splitter | converger | connector`。

### 1. 分流器（splitter）何时生成
- 从已有物流格沿**非原出口方向**拉出新分支 → 该格替换为分流器，入口边保持原物流的 inputEdge。
- 沿原出口方向继续画 = **合法复用**，不生成分流器（这是"延长"不是"分支"）。

### 2. 汇流器（converger）何时生成
- 新路径终点落在已有物流格，且该格**已有合法上游**（inputConnected）→ 替换为汇流器，出口边保持原物流的 outputEdge。
- **无合法上游** → 不建汇流器，把旧格替换为普通弯/直道（入口 = 新拉入方向、出口 = 原出口）——即"路径合并改形"，不硬造节点。

### 3. 桥接器（connector）何时生成 ⭐ 本库完全没有的能力
- 设备出口轴与旧物流轴**正交**，且首格沿设备输出轴**直穿** → 生成桥接器：新线从旧线上跨过去，旧线不断。
- 首格立即转弯 → **不用**桥接器，直接把旧格替换为弯道。
- 价值：正交穿越不需要绕路/分层，走线长度和失败率都降。

### 4. 头对头禁止
- 从已有物流段的出口方向**反向拉入**（lastCell.fromEdge === 对方 outputEdge）→ `overlap-existing-logistics` 非法。
- ⭐ 对 RwRoute 的意义：BFS 之前先做方向预检，能省掉一批"铺到头才发现连不上"的失败重试。

### 5. 双层物流
- 同一格可同时存在传送带与管道（互不干扰）；终点识别时若同格有**另一类**物流，跳过它继续找下层的同类目标。

## 二、仿真模型（喂 ⑤ 测试覆盖 / 吞吐预期）

- **tick 节奏**：标准 20 tick/秒；dense 精简引擎 4 或 2 tick/秒（动态档 20/10/4/2）；配方相位统一 0.5 秒。
- **每 tick 五阶段**：advance-devices → build-solve-graph → layered-reverse-solve → rotate-routing-cursors → settle-recipes。
- **分流器 = 轮询组 round-robin**（stage-4 rotate-routing-cursors）：
  - 轮询组按「节点、方向、物流类型、优先级」划分；
  - 游标推进规则：**实际发生过传输（moved）的边推进游标**；未连接的死端口跳过、不阻塞轮转；**空 tick（全组无传输）保留游标位置**——否则游标会与设备输出节奏同步锁死（源码注释记录了 2026-05-08 / 05-17 两次订正）。
  - ⭐ 对本库的意义：分流器 1 进 3 出的语义是**轮询均分**，不是按需分配。下游三路速率不同时，实际吞吐受游标节奏约束——排布器报告应把这一点写进分流器的说明里。

## 三、对本库的具体落点

1. **RwRoute 升级清单**（按收益排序）：
   - 正交穿越 → 自动上**桥接器**（现为绕路或失败）；
   - BFS 前加**头对头/方向预检**（省失败重试）；
   - 分流器报告加「round-robin 均分」语义说明；
   - 走线改用**逐格改写**语义（改一条线只重铺受影响格）——这是「局部交换 / 平移搜索」能高效迭代的前提。
2. **⑤ 测试**：分流链工况直接按 round-robin 语义写断言（三路轮询的节奏可静态推演：20 tick/秒 ÷ 传送间隔）。
3. **交叉证实**：IP 的"蓝图"也只是网页内布局数据、**不是游戏内蓝图码** —— 本库 `[做不到] 蓝图码` 结论成立。
4. 它给 AI 的 AIDocument 子仓库是私有仓库（404），注释中引用的《仿真运行原理》§5.4/§8/§9 拿不到；以上规则全部提炼自公开源码与注释。
